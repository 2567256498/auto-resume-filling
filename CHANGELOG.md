# 变更日志

本文件记录**封装体自身**的发布历史。「现在是什么版本」以 `SKILL.md` 的 frontmatter 为准，本文件不承担该职责；使用者在自己的项目里产生的事件，记在项目的变更档案，不写进本文件。两者性质不同（一个是分发物的发布史，一个是某次部署的事件史），不构成双真源。

---

## v3.0.2 · 2026-09-23 · 交付前评审修订

**版次依据**：本次全是**修订与口径统一**，没有整节增删或重排（§5.4 口径下取次版本）。其中第三项把投递台账的形态从 v3.0.1 刚改成的 `.md` 回退为 `.xlsx`——对"当天已按 v3.0.1 初始化过"的环境属形态变化，因此第十段加了迁移提示；若认为该回退应按破坏性变更处理，改记主版本即可。

**怎么来的**：把 v3.0.1 整包取回、逐文件读，并在本机实跑两支脚本做验收。**五条里有四条属「照着做就会做错、而且都不报错」**——与 v3.0.0 记录的是同一类失效，只是这次长在包自己的外围。

### 一、复制配方的目标指到了包自己

| # | 现象 | 为什么是问题 | 修订 |
|---|---|---|---|
| 19 | `templates/CODEBUDDY.md` 第 6、13、90 行：配方取用的②「复制到 `__RECIPES_DIR__/<名>-autofill/`」。而 `__RECIPES_DIR__` 的取值**就是包内的 `recipes/` 目录**（`init-workspace.py` 的 `guess_paths`） | 生成给使用者的项目级要求文件里，①「就地引用包内 `recipes/`」与②「复制到…」落到**同一个目录**——照做等于原地打转，而「复制」正是本次要确立的第二个选项，被这句话直接否掉 | 三处改为复制目标 `__SKILLS_DIR__/<名>-autofill/`（项目自己的技能目录）；该占位符模板里本来就有，原先只用在 §〇.2 |
| — | 机械校验**抓不到** | 第 ⑨ 段只查「模板占位符 ↔ 脚本替换表双向闭合」，两个占位符都在替换表里 → 闭合成立、照旧 0 FAIL。它能保证「不残留 `__XXX__`」，**保证不了「填对了位置」** | 不新增机械判据（位置语义无法只看占位符名判定）；改为在模板里把两条路的**完整路径**并列写出，让人一眼能比 |

### 二、配方来源的两套说法（口径统一）

| # | 现象 | 为什么是问题 | 修订 |
|---|---|---|---|
| 20 | 同一本手册里两种口径并存：§1.1 配置项 5／§1.2／§1.5 说「二选一，默认就地引用包内 `recipes/`，增补写回那一份」；§1.4／§4.3／§4.6／§4.7／§5.2／§5.3 说「环境侧才是活跃落点、包内只是副本、不直接改包内」 | 使用者信哪一套都能走下去，但两套同时在同一本手册里＝手册不再有唯一答案；§4.3 末尾还专门加了一句「不要因为手册说环境侧才是活跃落点就以为项目侧空目录等于没有配方」——**承认了冲突却没消掉** | 全部统一到「二选一、默认就地引用包内、**只改生效的那一处**」：涉及 §1.4 维护面、§1.2 随行表、§4.3 前置层与末段、§4.6 分层表、§4.7 索引说明与「新系统沉淀路径」、§5.2 前置约束与落点表、§5.3 回写顺序，共 13 处措辞 |

### 三、投递台账：形态与对账必须同源

| # | 现象 | 为什么是问题 | 修订 |
|---|---|---|---|
| 21 | v3.0.0 把数据层收成「一份分节 Markdown」时，投递台账也一并改成 `info/投递台账.md`（附录 C 是 Markdown 表、脚本按模板块抽取）；但 `verify-package.py` 的 `find_apply()` 只找 `*.xlsx` | 第十段的「投递记录对账（序号连续性）＝轮的锚点有没有断」在新环境里**永远不执行**：实测输出「跳过（未找到）」，不算 FAIL、不算 WARN、退出码 0——**以为查过了**。这正是本次要消灭的失效类型 | 形态回退为 **xlsx**：脚本内建表头行生成（`write_apply_xlsx`，不依赖 Excel／openpyxl，二进制无法用文本模板块承载）；附录 C 改为**列结构说明**；初始化清单与 README 同步写明 xlsx |
| 22 | 连带：投递台账回到 xlsx 后，新环境初态（只有表头、0 行）会命中 `not seqs` 分支，报 WARN「读不到序号列」 | 初态不是异常——照 v3.0.0 的验收要求「模板自证：初态即绿」，新环境一建出来就带 WARN 会让人以为装坏了 | `not seqs and not strange`（只有表头）判为**初态**，出 `info` 不出 WARN；只有「有行但读不出数字」才 WARN |
| 23 | 已在 v3.0.1 下初始化过的环境，投递台账是 `.md`，回退后校验器找不到 | 静默「跳过」＝使用者不知道自己的对账没生效 | 第十段新增迁移提示：找到 Markdown 形态的投递台账即 WARN，指明「转成 xlsx 后删掉该文件，否则投递序号对账不生效」 |

### 四、清理命令的静默成功

| # | 现象 | 为什么是问题 | 修订 |
|---|---|---|---|
| 24 | `--clean-tmp` 在项目根**不存在**时（路径拼错）打印「未建 tmp/（本项目还没有临时产物）」并退出码 0 | 使用者会以为已经清完。同一支脚本的 `--check` 在同样输入下报缺失 5 项、退出码 1——两者行为不一致，且与本版反复强调的「缺了不报错是最危险的失效」相悖 | `clean_tmp` 先判项目根是否存在，不存在即打印 FAIL 并返回 1 |

### 五、失效引用与小瑕疵

| # | 现象 | 修订 |
|---|---|---|
| 25 | 手册与初始化脚本共 5 处引用「§1.5 步骤四」，而 §1.5 只有「步骤一（机器生成骨架）／步骤二（使用者填内容）」——**该编号不存在**（v2.9.1 即存在，v3.0.0 整节重写 §1.5 时未修） | 统一改指 **§1.1 配置项 5**（配方目录二选一的要求本体所在处） |
| — | 第 ⑥ 段判据只认 `§数字` 形式，「§1.5 步骤四」里的「步骤四」不在判据内 → 校验盲区 | 不新增机械判据（叙述式引用无法穷举）；记明此处，改条文时按 §5.3 第 6 步先查该 ID 的变动史 |
| 26 | 生成的项目级要求文件首行「本文件是**本报**项目级要求」 | 改「本项目」 |
| 27 | 模板 §〇.4「去个人化由包内机械校验的个人数据段**机械**拦」 | 去重复措辞 |
| 28 | moka 配方里两处点名具体企业（一份职位申请表的实例出处、《…招聘隐私政策》） | 改为「某通信设备企业校招门户」，保留 `applyFormId` 识别坐标；页面层识别事实不受影响 |

### 验收（本机实测，退出码均 0 除注明者）

- **封装体档**（九段 ＋ 运行态）：**FAIL 0 / WARN 0，退出码 0**；
- **模板自证**（干净目录照模板生成）：生成 5 项产物，`info/投递台账.xlsx` 为合法 xlsx（5 个部件、表头 7 列正确），**占位符残留 0、反斜杠残留 0**；
- **配方路径分叉验证**：生成物里①＝包内 `recipes/<名>-autofill/`、②＝`<项目根>/.workbuddy/skills/<名>-autofill/`，两条路径**已不同**；
- **对账生效验证**：给投递台账塞入序号 `1/2/4` → 第十段报 `FAIL 投递记录序号不连续（缺 [3]）`、退出码 1；清空成只有表头 → 报「初态（0 行，仅表头）」、不报错；
- **迁移提示验证**：只留旧形态 `投递台账.md` → 出现 WARN 迁移提示；
- **清理命令**：项目根不存在 → `FAIL 项目根不存在`、退出码 1；正常项目 → 只清 `tmp/`，`evidence/` 与 `backup/` 零触碰（造三目录探针实测）。

### 未决

- 「复制配方」这条路线在多数环境里其实是多余的（包内 `recipes/` 已被技能扫描器注册成技能），但保留它是因为包可能被放在扫描路径之外——**是否把②降为兜底、①写成唯一默认**，等下一轮实测再定。

---

## v3.0.1 · 2026-09-23 · 信息目录与临时产物清理

**版次依据**：本次只**新增目录形态约定与一个新脚本动作**（`--clean-tmp`），不改已有配置项语义、不动写入通道，属**向后兼容的增量**，故取次版本。来源与 v3.0.0 同一轮实测：使用者（封装体作者）跑完之后提了两条要求——**信息文件别散在项目根**、**临时文件别一轮轮堆积**。

### 六、文件落点：设计只管了「临时的」，没管「长期的」

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 16 | 一轮任务下来，项目根与数据层同层堆着：数据层、投递台账、两份废弃占位、简历 PDF、证件照、5 张页面截图、启动器 | 手册只在「运行产物落点」一条里规定了**临时产物**的去处，**信息文件本身没有目录归属**——而「别散在数据层旁边」这句，在附件与历史占位身上同样成立，却无人管 | 引入**信息目录**（＝数据层所在目录，默认 `<项目根>/info/`）：数据层、投递台账、其余信息文件与附件原件同置该目录，**项目根只留要求文件与启动器**；**不新增配置项**（信息目录由数据层路径推出，避免多一个要维护的常量）；初始化脚本建目录并在 `--plan` 里展示；生成的要求文件 §〇.1 落成硬性要求 |
| 17 | `.workbuddy/tmp/` 里堆着 175 个散落文件 ＋ 5 个目录（2.1 MB）：探查脚本、回读输出、旧备份、自证目录混在一起 | 旧口径只写「收尾时清一次」，**既没有清理动作、也没有机械执行手段**，实测等于不清；更要紧的是「要清的」与「要留的」（改前留档、截图证据）混在同一目录，结果是**一律不敢删** | ① 三目录分工：`tmp/`（临时·**清**）、`evidence/`（过程证据·留）、`backup/`（改前留档·留）；② 新增 `init-workspace.py --clean-tmp`——先印清单、只清 `tmp/`、**绝不触碰**另两者；③ 收尾清单与要求文件同步改为「**必须清空** `tmp/`」，不再是"清一次"这种没有动作的软措辞 |
| 18 | 生成物打印建目录结果时 `\` 与 `/` 混用（本次改动新引入，自证时当场发现） | 既有的分隔符归一函数只覆盖了要求文件的替换表，**新写的输出路径没走它** | 建目录与末屏提示统一走该函数 |

### 一个被显式化解的张力

旧纪律说散落的临时产物「**只移不删**」——那是在「分不清哪些能删」的前提下定的保险做法。本轮把目录按**留与不留**切开之后，这条张力消失：`tmp/` 的内容**天生就是要清的**，`evidence/` 与 `backup/` **天生就是要留的**，于是「清理」不再需要逐个判断，也不会误伤回滚凭据。「只移不删」退化为**首次归位时**的过渡做法。

### 验收（本次实测）

- **封装体档九段**：FAIL 0 / WARN 0，退出码 0（含「模板占位符 ↔ 脚本替换表双向闭合」——新占位符已同步）；
- **模板自证**：干净目录生成 9 项（4 目录 ＋ 5 文件）、FAIL 0；占位符零残留、反斜杠零残留，且数据层与投递台账**均落在 `info/` 下**；
- **本项目部署体检**：0 缺失 / 5 必检，退出码 0；
- **本项目每轮档**：FAIL 0 / WARN 2（初态正常：轮次基准尚空、抖动窗口未声明），退出码 0；
- **`--clean-tmp` 实测**：清空 `tmp/` 后，`evidence/` 与 `backup/` 的文件数不变（脚本只报 KEEP、不删）。

---

## v3.0.0 · 2026-09-23 · 新环境首次完整实测修订

**版次依据**：按手册 §5.4 的版本规则，本次含**破坏性形态变更**（数据层的给定形态、机械校验的默认行为都变了），且属**对外发布节点**，故取主版本；若只按「子节增删」的字面口径，本可记为次版本。

### 实测怎么做的

把封装体拿到**另一个工作环境**，从零跑完整链路：部署初始化 → 建数据层 → 开工体检 → 认系统 → 取配方 → 逐区块填一份真实的职位申请表（个人信息、教育背景、校园／实习／项目经历、竞赛、荣誉、编程语言、外语、技能证书、自我评价、附件与确认声明）→ 整页回读核对 → 收尾闭环。

一句话结论：

> **硬骨头啃下来了——写入通道、控件定位、回读核对一次到位；问题几乎全出在封装体的外围：初始化不做检查、真源不止一处、治理机制比它要保障的操作还重。**

以下按「与设计预期不符」的程度排列。前四条只有真跑一遍才会撞上，光读文档看不出来。

### 一、部署与初始化：设计说生成了，实际没生成

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 1 | 使用者主动问起「投递台账做了吗」时，才发现**该文件根本不存在** | 初始化清单里**没有投递台账**，而它是这套流程里**唯一使用者会主动翻**的产出；更要命的是**开工流程里没有任何一步会发现它缺失**——未初始化的项目永远轮不到初始化那一步 | 生成清单加项 ＋ 新增「开工第 0 步 · 部署体检」（只读检查，缺项即补）＋ 初始化脚本纳入必检 ＋ 新增附录模板块 |
| 2 | 数据层出现**两个真源**：使用者已填的 Markdown 库，与初始化刚生成的空骨架 | 配置项只给到**目录**级（占位符本身就叫「数据目录」），**没有形态约定**；而「两处各写一份」正是设计里最怕、且**两边都不会报错**的失效 | 配置项改**点名单文件**；骨架由两份改为**一份分节 Markdown**；并明确**禁止在配置项里举文件名示例**——举例会被下一次照抄成骨架文件名 |
| 3 | 效力顺序的第二档指向一个**本机不存在的目录** | 脚本把用户级要求文件的候选路径**写死**了；生成物里看不出异常，只是安静地指向空气 | 改四路探测（两个候选目录 ＋ 两份候选文件），全无则写「无（本机未见用户级要求文件）」 |
| 4 | 两个占位符**共用同一个取值**（技能目录与配方目录） | 一旦把配方目录改指为包内，台账路径会被顺手写错，而**校验器抓不到**（两者都「有值」） | 拆成两个独立取值 |
| 5 | 生成物里路径分隔符 `\` 与 `/` 混用 | 跨环境复制时是隐患（同一条路径两种写法） | 加统一函数，全部归一为正斜杠 |

### 二、配方来源：单源假设在新环境不成立

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 6 | 项目侧配方目录是空的，而规则要求「包内配方不作本项目手法来源」→ **唯一的可用来源被禁掉** | 设计假设「配方放在环境侧、包内只是分发副本」；但包本身就装在技能扫描目录内，`recipes/*/SKILL.md` **已被注册成技能可直接读**。若照初始化步骤再复制一份到项目侧，就会变成**同名技能两份** | 多处口径统一为「**二选一 ＋ 同一环境同名配方只能存在一份**」；未复制时包内即为有效来源；并写明代价（就地写回会让包累积本项目知识，整包覆盖升级前需先把增补抄出） |

### 三、治理机制的配重：最核心的一条

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 7 | 经验闭环与机械校验这套机制，在**一次完整任务里一次都没运转**；审计后：闭环一节占手册约三分之一，其中过半是**要复制出去的模板正文**；校验器九段里**七段查的是「封装体自己是否自洽」**，对「这次填报对不对」几乎不产生判断 | 机制本身没错，但**配重错了**：维护者项被压进使用者的每一轮收尾，而阈值／休眠／折叠这类机制对单人跑网申明显过重 | ① 经验机制分**轻量档（默认）／完整档**，档位写在台账里，未声明按完整（向后兼容）；② 校验器分**每轮档（默认）／封装体档**；③ 完整档下连续三轮已复核而无新增事件 → 提示复核机制是否过重（只提示不阻断）；④ 模板正文移出手册正文 |
| 8 | 模板正文占手册正文过长的一截，是「治理重于操作」的直接来源 | 设计要求「模板只留一处真源」，于是正文说明与模板挤在一起 | 模板整体移至**附录**，就地抽取路径不变——单真源的设计得以保留，正文回到约五百行 |

### 四、校验器自身的缺陷：全是实测撞出来的

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 9 | 手册文档化了一条「部署者可自建禁用词表」的流程，**照做一建就 FAIL** | 校验器扫全包时**没排除那张词表自身**，词表里列的敏感词把它自己命中了 | 扫描时排除自身 |
| 10 | 包目录建版本库后，封装体档 **FAIL 0 变 FAIL 50** | 全包扫描把版本控制元数据当交付物扫：配置里报「疑似邮箱」、对象库里报「编码无法解析」（对象是压缩二进制，当文本读必失败） | 目录遍历剪枝扩至版本控制元数据；并在手册写明随之而来的**边界**——只存在于版本历史、已从工作区删掉的内容**扫不到**，发布前需另行检查版本历史 |
| 11 | 修订封装体的过程中，**把使用者本人的数据写进了包**（成绩、院校、雇主、专业、绩点） | 校验器**确实拦住了，但只拦到一部分**——它揪出「分数」这一种形状，同一段里的院校、雇主、专业全数漏网。这说明形状类判据**天然有可达边界** | 整段去个人化，包内残留归零；判据维持「取形状不取具体值」的立场，并在手册中点明这类判据的边界 |

### 五、真实填报中的实现问题

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 12 | 配方里已写明「上传简历会触发自动解析、有覆盖风险」，**事故仍然发生了**：使用者手动点了它，教育背景被清空、实习 4 条掉 3 条、项目 2 条掉 1 条、自我描述从近五百字掉到五十字 | **只在文档里写风险不够**——风险提示的落点必须在「使用者动手的那一刻」，而不是躺在配方文件里 | 配方补完整的**事故修复流程**：基线快照 → 补回条目行 → 覆盖式回填 → 清残留校验错误 → 与基线逐字段比对验收 |
| 13 | 解析残留的旧值让回填写成**首尾相接的拼接串** | 逐字追加式写入在「字段已有值」时不成立 | 定为硬口径：**回填一律先全选再插** |
| 14 | 解析留下可见的红色「必填项未填写」错误（字段明明有值）；重设值、真实点选、派发 `blur` **全都清不掉** | 这类错误是**校验快照**，不随字段变更自动复位；而 React 的 `onBlur` 实际监听的是原生 `focusout`，不是 `blur` | 记入配方：派发原生 `focusout` 触发重新校验，一次清零 |
| 15 | 「同一门户只有一套申请表」的既有记载被证伪：**同一门户存在两套差异极大的模板，多出的区块里有若干必填** | 配方按「实例」记录模板，容易被后来者当作该门户的唯一形状照抄 | 配方写明：**开工前必须按页面上的区块标识现场普查**，不得照抄任一实例的区块表 |

### 六、操作纪律：执行侧的两条

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 16 | 探查脚本与输出散落在**数据层所在的目录**里，最多时近百个文件，其中十余个含证件号码 | 设计没有规定运行产物的落点，而「数据层是单文件」的约定又让这个目录看起来「都是重要文件」 | 新增落点纪律（运行产物一律落项目临时目录）＋ 收尾清单项 ＋ 要求文件同步一条；已有的散落文件**只移不删**归位 |
| 17 | 同一条消息里对**同一个文件**并行下发多处编辑 → **互相覆盖，每次仍回「成功」**，改动静默丢失。本次会话**复现两次** | 工具的返回语义（「成功」）与实际结果（部分丢失）不一致，只能靠回读发现 | 定为硬纪律：同一文件的多处改动一律用**单次原子脚本**，并逐处核对；不得依赖并行编辑 |

### 验收

改完按手册的自证要求跑了四项机械判据，全部通过、退出码 0：

- **部署体检**：必检产物 0 缺失；
- **每轮档**：FAIL 0（两条提示属初态正常：轮次基准尚空、抖动窗口未声明）；
- **封装体档**：FAIL 0；
- **模板自证**（把模板当输入跑一遍）：在干净目录照新模板生成骨架，**初态即绿**，占位符零残留、反斜杠零残留，生成台账的档位行确认为轻量档；
- **分档行为探针三项**：完整档＋缺待验表 → 拦下（非零退出）；完整档＋连续三轮无事件 → 提示；轻量档＋缺待验表 → 放行。

### 未决（留待后续实测）

- 论文／专利／软件著作权这类「记忆里确实没有」的区块，是留空还是显式写「无」，两种处置的观感与通过率未见分晓；
- 手工上传类控件（照片／附件）在本通道仍无命令可用，须使用者手动完成——**这是设计接受的边界，不是缺陷**，但「提示在什么时刻给出」值得再想；
- 数据层里若干口径互不一致（同一事实两处记录写法不同）时，当前处置是「以数据层为准并回读比对、冲突即停下问」；是否要加一条**数据层自洽体检**，待观察。

---

本封装体此前为内部迭代，无对外发布记录，故不追记更早版本。
