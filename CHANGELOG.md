# 变更日志

本文件记录**封装体自身**的发布历史。「现在是什么版本」以 `SKILL.md` 的 frontmatter 为准，本文件不承担该职责；使用者在自己的项目里产生的事件，记在项目的变更档案，不写进本文件。两者性质不同（一个是分发物的发布史，一个是某次部署的事件史），不构成双真源。

---

## v3.0.0 · 2026-09-23 · 新环境首次完整实测修订

**版次依据**：按手册 §5.4 的版本规则，本次含**破坏性形态变更**（数据层的给定形态、机械校验的默认行为都变了），且属**对外发布节点**，故取主版本；若只按「子节增删」的字面口径，本可记为次版本。

### 实测怎么做的

把封装体拿到**另一个工作环境**，从零跑完整链路：部署初始化 → 建数据层 → 开工体检 → 认系统 → 取配方 → 逐区块填一份真实的职位申请表（个人信息、教育背景、校园／实习／项目经历、竞赛、荣誉、编程语言、外语、技能证书、自我评价、附件与确认声明）→ 整页回读核对 → 收尾闭环。

一句话结论：

> **硬骨头啃下来了——写入通道、控件定位、回读核对一次到位；问题几乎全出在封装体的外围：初始化不做检查、真源不止一处、治理机制比它要保障的操作还重。**

以下按「与设计预期不符」的程度排列。前四条只有真跑一遍才会撞上，光读文档看不出来。

### 一、部署与初始化：设计说生成了，实际没生成

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 1 | 使用者主动问起「投递台账做了吗」时，才发现**该文件根本不存在** | 初始化清单里**没有投递台账**，而它是这套流程里**唯一使用者会主动翻**的产出；更要命的是**开工流程里没有任何一步会发现它缺失**——未初始化的项目永远轮不到初始化那一步 | 生成清单加项 ＋ 新增「开工第 0 步 · 部署体检」（只读检查，缺项即补）＋ 初始化脚本纳入必检 ＋ 新增附录模板块 |
| 2 | 数据层出现**两个真源**：使用者已填的 Markdown 库，与初始化刚生成的空骨架 | 配置项只给到**目录**级（占位符本身就叫「数据目录」），**没有形态约定**；而「两处各写一份」正是设计里最怕、且**两边都不会报错**的失效 | 配置项改**点名单文件**；骨架由两份改为**一份分节 Markdown**；并明确**禁止在配置项里举文件名示例**——举例会被下一次照抄成骨架文件名 |
| 3 | 效力顺序的第二档指向一个**本机不存在的目录** | 脚本把用户级要求文件的候选路径**写死**了；生成物里看不出异常，只是安静地指向空气 | 改四路探测（两个候选目录 ＋ 两份候选文件），全无则写「无（本机未见用户级要求文件）」 |
| 4 | 两个占位符**共用同一个取值**（技能目录与配方目录） | 一旦把配方目录改指为包内，台账路径会被顺手写错，而**校验器抓不到**（两者都「有值」） | 拆成两个独立取值 |
| 5 | 生成物里路径分隔符 `\` 与 `/` 混用 | 跨环境复制时是隐患（同一条路径两种写法） | 加统一函数，全部归一为正斜杠 |

### 二、配方来源：单源假设在新环境不成立

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 6 | 项目侧配方目录是空的，而规则要求「包内配方不作本项目手法来源」→ **唯一的可用来源被禁掉** | 设计假设「配方放在环境侧、包内只是分发副本」；但包本身就装在技能扫描目录内，`recipes/*/SKILL.md` **已被注册成技能可直接读**。若照初始化步骤再复制一份到项目侧，就会变成**同名技能两份** | 多处口径统一为「**二选一 ＋ 同一环境同名配方只能存在一份**」；未复制时包内即为有效来源；并写明代价（就地写回会让包累积本项目知识，整包覆盖升级前需先把增补抄出） |

### 三、治理机制的配重：最核心的一条

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 7 | 经验闭环与机械校验这套机制，在**一次完整任务里一次都没运转**；审计后：闭环一节占手册约三分之一，其中过半是**要复制出去的模板正文**；校验器九段里**七段查的是「封装体自己是否自洽」**，对「这次填报对不对」几乎不产生判断 | 机制本身没错，但**配重错了**：维护者项被压进使用者的每一轮收尾，而阈值／休眠／折叠这类机制对单人跑网申明显过重 | ① 经验机制分**轻量档（默认）／完整档**，档位写在台账里，未声明按完整（向后兼容）；② 校验器分**每轮档（默认）／封装体档**；③ 完整档下连续三轮已复核而无新增事件 → 提示复核机制是否过重（只提示不阻断）；④ 模板正文移出手册正文 |
| 8 | 模板正文占手册正文过长的一截，是「治理重于操作」的直接来源 | 设计要求「模板只留一处真源」，于是正文说明与模板挤在一起 | 模板整体移至**附录**，就地抽取路径不变——单真源的设计得以保留，正文回到约五百行 |

### 四、校验器自身的缺陷：全是实测撞出来的

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 9 | 手册文档化了一条「部署者可自建禁用词表」的流程，**照做一建就 FAIL** | 校验器扫全包时**没排除那张词表自身**，词表里列的敏感词把它自己命中了 | 扫描时排除自身 |
| 10 | 包目录建版本库后，封装体档 **FAIL 0 变 FAIL 50** | 全包扫描把版本控制元数据当交付物扫：配置里报「疑似邮箱」、对象库里报「编码无法解析」（对象是压缩二进制，当文本读必失败） | 目录遍历剪枝扩至版本控制元数据；并在手册写明随之而来的**边界**——只存在于版本历史、已从工作区删掉的内容**扫不到**，发布前需另行检查版本历史 |
| 11 | 修订封装体的过程中，**把使用者本人的数据写进了包**（成绩、院校、雇主、专业、绩点） | 校验器**确实拦住了，但只拦到一部分**——它揪出「分数」这一种形状，同一段里的院校、雇主、专业全数漏网。这说明形状类判据**天然有可达边界** | 整段去个人化，包内残留归零；判据维持「取形状不取具体值」的立场，并在手册中点明这类判据的边界 |

### 五、真实填报中的实现问题

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 12 | 配方里已写明「上传简历会触发自动解析、有覆盖风险」，**事故仍然发生了**：使用者手动点了它，教育背景被清空、实习 4 条掉 3 条、项目 2 条掉 1 条、自我描述从近五百字掉到五十字 | **只在文档里写风险不够**——风险提示的落点必须在「使用者动手的那一刻」，而不是躺在配方文件里 | 配方补完整的**事故修复流程**：基线快照 → 补回条目行 → 覆盖式回填 → 清残留校验错误 → 与基线逐字段比对验收 |
| 13 | 解析残留的旧值让回填写成**首尾相接的拼接串** | 逐字追加式写入在「字段已有值」时不成立 | 定为硬口径：**回填一律先全选再插** |
| 14 | 解析留下可见的红色「必填项未填写」错误（字段明明有值）；重设值、真实点选、派发 `blur` **全都清不掉** | 这类错误是**校验快照**，不随字段变更自动复位；而 React 的 `onBlur` 实际监听的是原生 `focusout`，不是 `blur` | 记入配方：派发原生 `focusout` 触发重新校验，一次清零 |
| 15 | 「同一门户只有一套申请表」的既有记载被证伪：**同一门户存在两套差异极大的模板，多出的区块里有若干必填** | 配方按「实例」记录模板，容易被后来者当作该门户的唯一形状照抄 | 配方写明：**开工前必须按页面上的区块标识现场普查**，不得照抄任一实例的区块表 |

### 六、操作纪律：执行侧的两条

| # | 实测现象 | 与设计预期的偏差 | 优化 |
|---|---|---|---|
| 16 | 探查脚本与输出散落在**数据层所在的目录**里，最多时近百个文件，其中十余个含证件号码 | 设计没有规定运行产物的落点，而「数据层是单文件」的约定又让这个目录看起来「都是重要文件」 | 新增落点纪律（运行产物一律落项目临时目录）＋ 收尾清单项 ＋ 要求文件同步一条；已有的散落文件**只移不删**归位 |
| 17 | 同一条消息里对**同一个文件**并行下发多处编辑 → **互相覆盖，每次仍回「成功」**，改动静默丢失。本次会话**复现两次** | 工具的返回语义（「成功」）与实际结果（部分丢失）不一致，只能靠回读发现 | 定为硬纪律：同一文件的多处改动一律用**单次原子脚本**，并逐处核对；不得依赖并行编辑 |

### 验收

改完按手册的自证要求跑了四项机械判据，全部通过、退出码 0：

- **部署体检**：必检产物 0 缺失；
- **每轮档**：FAIL 0（两条提示属初态正常：轮次基准尚空、抖动窗口未声明）；
- **封装体档**：FAIL 0；
- **模板自证**（把模板当输入跑一遍）：在干净目录照新模板生成骨架，**初态即绿**，占位符零残留、反斜杠零残留，生成台账的档位行确认为轻量档；
- **分档行为探针三项**：完整档＋缺待验表 → 拦下（非零退出）；完整档＋连续三轮无事件 → 提示；轻量档＋缺待验表 → 放行。

### 未决（留待后续实测）

- 论文／专利／软件著作权这类「记忆里确实没有」的区块，是留空还是显式写「无」，两种处置的观感与通过率未见分晓；
- 手工上传类控件（照片／附件）在本通道仍无命令可用，须使用者手动完成——**这是设计接受的边界，不是缺陷**，但「提示在什么时刻给出」值得再想；
- 数据层里若干口径互不一致（同一事实两处记录写法不同）时，当前处置是「以数据层为准并回读比对、冲突即停下问」；是否要加一条**数据层自洽体检**，待观察。

---

本封装体此前为内部迭代，无对外发布记录，故不追记更早版本。
