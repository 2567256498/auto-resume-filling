---
name: qqbrowser-resume-fill
description: 简历网申填写任务手册（封装体：`README.md` 使用说明＋本文件＋`recipes/`＋`scripts/`＋`templates/`，可整体移交）——首次部署初始化、部署配置与移交说明、任务定义与全部硬性要求、QQ 浏览器通道规范、流程与配方分层、经验固化闭环、包内机械校验、自检与失败模式。用于执行网申／招聘表单填写，或通读、修订、移交这套任务。
version: v3.1.2
updated: 2026-09-24
agent_created: true
---

# 简历网申填写 · 任务手册

> **封装体与移交用法**：`README.md`（使用说明——面向接手者的简版上手，与本手册冲突时以本手册为准）＋ 本文件 ＋ `recipes/` ＋ `scripts/`（启动器模板、初始化脚本、包内机械校验）＋ `templates/` 构成一个可整体移交的封装——整份复制到目标位置（**并为网申任务单单开一个专用目录，即下文的「项目根」**）→ **跑一次 `python scripts/init-workspace.py --plan`**（§1.5：生成项目级要求文件、台账与变更档案、数据层骨架；**或按引导由使用者生成**）→ 按 §1.1 填一次配置 → 跑一次 `python scripts/verify-package.py`（§6.3）→ 即可开工。§二–§六 与环境无关，只有 §一 的路径需按新环境替换。
> **不含个人数据**：本封装体只写“怎么做”，不写“填什么”——一切取值与专属口径都在使用者的数据层；随包不携带任何个人经历、成绩、院校、联系方式或投递记录。数据层在新环境由 §1.5 生成**空结构**，内容一律由使用者填。
> **版本**：见 frontmatter 的 `version` 与 `updated`（本手册内不再重复版本号）；历史不写在本手册内，见变更档案（配置项 6）；本手册自身的迭代规则见 §5.4。
> **权威关系**：对本封装体的使用者而言，**本手册就是唯一要求来源**——不镜像、不同步任何外部要求文件。若目标环境另有要求文件，视为更高优先且**只读**：冲突时以它为准并报使用者，由使用者裁定后当场修正本手册（§1.4）。

## 一、部署配置与移交

### 1.0 前置环境（通道侧 ＋ 脚本侧，新环境先过这一节）

**分工**：本节的**前置**分两块，**都要先过**——**A 通道侧**（驱动浏览器的那件工具与那个浏览器**在不在、可不可用**）＋ **B 脚本侧**（跑包内脚本要的**解释器**在不在）；§1.5 建**项目侧**产物。**A、B 与 §1.5 的产物都不随包、都要先确认；先过本节，再过 §1.5。**

**A · 通道侧**。网申填写依赖**一件通道 CLI ＋ 一个桌面浏览器**，两者都**不在包内、也不随包分发**（配置项 1 点名它们是哪一个）。判据分三段，**有序、且不许跳段**：

| # | 要什么 | 判据（只读，不碰页面） | 不过时怎么办 |
|---|---|---|---|
| ① | 通道 CLI 能解析 | 能打印用法（如 `<CLI 可执行文件> --help`，退出码 0；**本通道 CLI 无 `--version`**，2026-09-23 实测） | **停在本段**：去装，入口见下条 |
| ② | 浏览器本体在运行 | 进程表里真有浏览器主进程（只读查：`tasklist` 找浏览器 exe）**且**由**使用者在自己桌面会话**启动并保持。**命令「能跑」不等于浏览器在运行**——浏览器不在时 CLI 会临时拉起一个、命令结束即回收，症状见 §6.2 | 请使用者启动；**不自行拉起**（§3.0.3） |
| ③ | 内置扩展已接入 | 启动器日志出现 `Connected clients: 1` | 查启动参数（§3.0 前提二）；**不自行拉起** |

**为什么必须分成三段**：① 不过（`命令找不到`）与 ② 不过（命令一律 60 秒超时）是**两种不同的故障**，处置完全不同——**「装没装」不等于「可不可用」**。§3.0 的症状对照针对的是 ②③ 段；把它套到 ① 段上，会把排查方向引向浏览器。

**① 段：`qqbrowser-skill` 指什么形态**。本节之后以及 §3.2 命令表里出现的 `qqbrowser-skill`，一律指**可解析的调用形式**，按下列优先级择一（与配方内 `scripts/run_cli.py` 的约定一致）：

1. 环境变量 `QQBROWSER_SKILL` 指向 CLI 可执行文件；
2. CLI 可执行文件的**绝对路径**；
3. 环境 `PATH` 里的裸命令名。

**裸命令名不可靠**：本机实测它就不在 `PATH` 里——§3.0.4 记的那次升级失败（`WinError 2`）正是升级器用了裸命令名。**所以别照抄命令表里的字面形式直接执行**，先确认这条命令在你的环境里解析得到。

**来源与版本不写死，但入口要接上**：包**不携带**任何可执行文件，也**不写死版本号与安装包直链**——直链会失效、版本会迭代，写死即过时。**但 ① 段不过时总得知道去哪装**，故给两个**官方入口**，包内只到这一粒度：

- **通道 CLI**：PyPI 项目页 `pypi.org/project/qqbrowser-skill/`（装法以该页当前给出的为准）。
- **浏览器本体**：官方站 `browser.qq.com`（亦即通道包元数据里自报的 Homepage）。

**入口只写「去哪装」，不写「哪一版」**：项目页随发版更新，故包内不抄版本号、不抄安装包直链，**一律以入口页当前内容为准**——这样入口不会因迭代而失效。来源与装法仍由使用者按其环境决定；**换通道＝换 §三 整节**（§1.3）。装完只认上面三段判据。**部署体检（§1.5）会只读探一次本段**：探不到就当场报「通道未就位」并列出上面两个入口。

**B · 脚本侧（解释器）**。§1.5 建骨架、§6.3 机械校验、§3.7.11 收尾清理都由包内脚本完成，脚本要一个能跑 **Python 3** 的解释器。**但这一项不用你另外安装任何东西——它已经随着通道装进来了**：

- **不必再装 Python**（结论先行）：通道 CLI 本身就是 pip 包（元数据 `Requires-Python >=3.10`）——**能把它装上，就说明这台机器上已经有一个 3.10 及以上的 Python**。本机实测 `qqbrowser-skill.exe` 与 `python.exe` 就躺在**同一目录**，脚本直接用那个解释器即可。**不要再去官网下另一个**：多版本并存只会让「该用哪个」变含糊（本机就有两个：受管 3.13.12 与 anaconda），而挑错版本比没有更像故障。
- **版本**：**3.10 及以上**（与通道 CLI 同一门槛；实测环境为 3.13，更低版本未实测）。这一条由上行「通道已带来」自然满足，单列只为把判据写全。
- **依赖**：**零**。包内三个脚本（`init-workspace.py`／`verify-package.py`／配方内 `run_cli.py`）**只用标准库**，**不需要 `pip install` 任何东西**——`verify-package.py` 自己解析 xlsx，正为避开 `openpyxl`。
- **唯一要留神的是调用形态**：本节及 §1.5／§3.7.11／§6.3 命令里的 `python` 是**占位名**，按「环境变量 `PYTHON` ＞ 绝对路径 ＞ 裸命令名」择一，**首选与通道 CLI 同目录那个解释器**；**裸 `python` 不可靠**（本机实测：同一目录下 `python` 能解析而 `qqbrowser-skill` 不能，纯属偶然）。包内脚本一律以**封装体内的相对路径**调用（`scripts/...`），别把绝对路径写进手册。
- **真的一个解释器都找不到时**（装了通道却搜不到 Python，极罕见——**只有 `python` 命令确实解析不到，才需要看这一条**）：骨架可按 §1.5 的模板**手工生成**，但 `--check`（开工体检）、`--clean-tmp`（收尾清理）与机械校验**没有手工等价物**——等于放弃「能交给机器查证的，就不靠人记」这条原则，而那正是 §5.4 记过的人眼必漏事故的来源。**处置顺序不是「先装 Python」**：先按上行确认通道那个解释器的位置（同目录、或 `PYTHON` 环境变量），确认真的没有，再谈安装。

### 1.1 部署配置表（唯一需要按环境修改的地方）

| # | 配置项 | 说明 | 取值（移交时填一次） |
|---|---|---|---|
| 1 | 通道驱动 | 驱动浏览器的 CLI 与其接管的浏览器 | `qqbrowser-skill` CLI（**1.6.0 实测**，2026-09-16），接管 **QQ 浏览器**；换浏览器＝换 §三 整节（详见 §1.3） |
| 2 | 使用者要求文件（**可选**） | 使用者自己的更高优先要求 | 路径由使用者提供（项目级／使用者级均可）。**只读、不镜像**：冲突时以它为准并报使用者，裁定后修正本手册；无则本手册独立生效（§1.4） |
| 3 | 通道文件（只读参考） | 通道事实的独立记录（**可选**） | 本手册 §三 已自包含，此项通常留空 |
| 4 | 数据层·**单文件**（取值） | 权威取值来源，**必须存在**，且必须**点名到一个文件**（不是目录） | 使用者的资料库**主文件**（一份 Markdown，同时承载事实与定稿文本、各系统字段取值、附件与照片路径、成绩明细）。**写成目录级即构成双真源**——主数据与字段映射会各成一份，而两份不一致时两边都不会报错（本机实测发生过：按目录生成骨架后，环境里并存两份数据层）。**本项一律写文件的绝对路径**；此处**不举文件名示例**，举例会被照抄成骨架文件名。写库走“先列单确认”（§5.2）。**新环境由 §1.5 生成骨架**（只建结构、不填内容）。**默认形态＝单个 Markdown 文件**（2026-09-23 裁定维持；改用 JSON 也须是单文件、并在此注明）。**数据层所在目录即信息目录**（2026-09-23 补）：投递台账、其余信息文件与附件原件同置该目录，**项目根只留要求文件与启动器**——信息文件散落根目录会让「哪一份才是数据层」重新变含糊；引用一律写该目录下的绝对路径 |
| 5 | 配方目录 | **二选一，且同一环境同名配方只能存在一份**：① **就地引用**（默认）＝包内 `recipes/`；② 复制到 `<配方根目录>/<名>-autofill/SKILL.md` | **两条路不能同时走**——包内 `recipes/*/SKILL.md` 在多数环境里会被技能扫描器直接注册成技能（本机实测：12 份配方全部在册、可直接读），再复制一份即出现**同名技能两份**、行为不可预期。选 ① 时，本项目对某配方的增补就写回包内那一份（去个人化由 §6.3 第⑦⑧段机械拦）；选 ② 时，包内那一份随之停用、不再作为来源。新系统配方按 §4.3 在**生效的那一处**新建 |
| 6 | 证据台账 | 经验台账与变更档案 | 路径由使用者提供（**不随包**）；**新环境由 §1.5 按 §5.1 模板直接生成** |
| 7 | 禁止触碰 | 本任务范围外的目录／文件（如与他人或其他通道共用的资产） | 按目标的边界填写；没有则留空 |
| 8 | 浏览器启动方式 | 浏览器可执行路径与**启动参数**（含内置扩展加载参数；缺则扩展不加载、命令 60 秒超时） | 由使用者提供；使用者侧可备「双击即用」启动器（要点见 §3.0.1）。机制、daemon 起法、升级陷阱见 §3.0 |

**取数唯一真源**：配置项 4。快照、记忆、上一轮读数、页面现状**一律不算**。

**前置 · 先为网申任务单开一个目录——即下文的「项目根」。** 本节的全部产出、此后的开工体检（§4.1 第 0 步）与收尾清理（§3.7 第 11 条）**都只对这一个目录操作**，所以要**新开一个专用于本任务的目录**（空目录即可，位置由使用者定），**不要跑在已有项目的目录、桌面或共用／同步目录里**。

三条理由：① 本节生成的**项目级要求文件对该工作区发起的全部会话生效**——跑错地方等于给无关项目套上网申要求；② 数据层、投递台账与附件原件都落在这里（信息目录），**这是使用者个人数据的家**，位置应由使用者有意选定、并便于自己掌握可见范围；③ 一个任务一个根——开工体检与收尾清理都只认这一个根，账才不串。**该目录由使用者决定，脚本既不代选、也难事后搬家**（生成物里的路径已写进要求文件与台账），故列为第 0 步。

配置项 4／6、**投递台账**及**项目级要求文件**在新环境都不存在、且都**不随包**——它们由 §1.5 一次性生成（或引导使用者生成）。 **信息目录**（＝数据层所在目录，默认 `<项目根>/info/`）与三类运行目录（`tmp/`／`evidence/`／`backup/`）也由 §1.5 一并建好——建之前它们都不存在，而缺了不报错。

### 1.2 移交通用性：哪些随行、哪些必须替换

| 内容 | 是否随行 |
|---|---|
| `README.md` | **随行**——面向接手者的使用说明（简版上手：怎么初始化、怎么填、一轮任务怎么走、出问题看哪）。**与手册冲突时以手册为准**；版本与历史不在此文件内重复 |
| §2.1 行为红线、§三 通道规范（同通道时）、§四 流程与分层、§五 闭环、§六 自检与失败模式、§4.7 判据对照与配方索引 | **通用，直接可用** |
| `recipes/` 全部配方 | **随行**——按 §1.1 配置项 5**二选一**：**就地引用**本包这一份（默认），或复制到环境侧目录。**同一环境同名配方只能存在一份**；就地引用时增补写回本包这一份，复制时本包这一份随之停用。内容分两类：**页面层手法**（页面内执行的脚本）跨通道通用，**可直接用**；只有依赖通道动作的三种步骤（真实点击／语义定位与 snapshot 索引／iframe 穿透）按【未复现】对待，能兜底的已写兜底；验到的结论就地在**生效的那一处配方**内回写（§5.3 第 5 条）；走复制路线时再同步进包。**配方自带的 `scripts/` 一并随行**——如 `feishu-atsx-autofill` 的 9 个文件（8 个页面脚本 ＋ 1 个本机运行器），清单与占位符约定写在该配方末节 |
| `scripts/launch-browser.bat` | **随行（模板）**——浏览器启动器；占位符按 §1.1 配置项 8 替换即可用；**未替换会在启动前中止并提示**，不会静默失败（要点见 §3.0.1） |
| `scripts/verify-package.py` | **随行**——包内机械校验，**两档**（§6.3）：**每轮档（默认）**＝运行态第十段 ＋ 模板自证；**封装体档**（`--package`）＝九段全跑 ＋ 运行态，改过封装体时才需要。每轮收尾必跑，退出码 0 才算完；**使用者环境的收尾校验也用它**（带 `--ledger` 指向本环境台账，生成的项目级要求文件 §七 即如此指向） |
| `scripts/init-workspace.py` ＋ `templates/` | **随行**——首次部署初始化（§1.5）：生成项目级要求文件、台账与变更档案、数据层骨架；**未替换占位符即中止**，不静默产出坏文件 |
| §2.2 内容填报口径 | **通用原则随包、专属取值不随包**——取值由数据层提供；数据层缺失时按 §2.2 五条通用原则执行 |
| §1.1 配置项 4 数据层、**投递台账**、配置项 6 台账与变更档案、项目级要求文件 | **不随行，但由 §1.5 生成骨架**——几项都是每个使用者／项目自己的资料库与任务史；新环境跑一次 `scripts/init-workspace.py` 即得结构（数据层只建骨架、内容须使用者填）。**投递台账单列一项**：它是**使用者唯一会主动翻的账**，不能只当数据层的附属（本机实测：上一轮初始化把它漏了，直到使用者来问才发现） |
| §1.1 配置项 5 配方目录 | **就地引用或复制，二选一**；**同一环境同名配方只能存在一份**。默认就地引用本包 `recipes/`，此时它就是唯一活跃落点 |

### 1.3 换浏览器通道要重建什么

- **只保留与通道无关的页面／系统层事实**，且必须标注"未在本通道复现"＋验证范围与确定度；**通道机制类证据一律重建**（定位写法、会话模型、故障模型、上传方式）。
- §三 整节替换为新通道规范；§四、§六 对应条目重写；配方条目重验。
- 参照经验：不同通道在**定位**（页面内 JS／CSS 选择器 vs 语义 locator／snapshot 索引）、**会话**（跟随使用者当前页 vs 独立标签组＋显式 start/end）、**故障**（超时崩进程 vs 命令返回错误不崩）三处可能完全相反。

### 1.4 与使用者资产的关系（不镜像）

- 使用者自有的要求文件**更高优先且只读**，冲突时以它为准并报使用者，裁定后当场修正本手册——不留双份说法。
- 本封装体的维护面只有三处：本手册（通用要求与流程）、**配方（生效的那一处）**（单框架／单系统手法；二选一见 §1.1 配置项 5——默认就地引用包内 `recipes/`，或复制到环境侧配方目录）、台账与变更档案（证据）；**改动的权限与版本规则见 §5.4**。

### 1.5 首次部署初始化（新环境开工前跑一次）

> **与 §1.0 的分工**：§1.0 判**前置**（**A 通道侧**＝CLI 与浏览器在不在；**B 脚本侧**＝Python 解释器在不在），本节建**项目侧**产物——都不随包、都要先确认，**先过 §1.0 再过本节**。

**为什么有这一节**：整份复制到新环境后，有五样东西**不在包里、环境里也没有**——① **项目级要求文件**；② 数据层（配置项 4）；③ 证据台账（配置项 6 前半）；④ 变更档案（配置项 6 后半）；⑤ **投递台账**。前三样缺了会直接出事：没有项目级要求文件，本手册的硬性要求就**不在启动时注入的上下文里**，模型"看不到要求"这件事在实测中已发生过（2026-09-19 事故背书：手册对流程规定完整，模型却未读、按字面理解动手）；没有数据层，"取数唯一真源"无从谈起；没有台账，§五 闭环没有落点；**没有投递台账**，「不擅自投递」与「轮的锚点」两件事同时落空——而它缺失时**不会报错**，只能等使用者来问（本机实测）。

初始化分**两步**，一步机器做、一步人做，**边界不许混**：

| 步骤 | 做什么 | 谁做 |
|---|---|---|
| 一 · 生成骨架 | 跑 `scripts/init-workspace.py`：产出项目级要求文件、台账、变更档案、数据层骨架 | 机器 |
| 二 · 填内容 | 把**简历事实与字段取值**逐项填进数据层 | **只能使用者做** |

**命令**：

```bash
python scripts/init-workspace.py --plan --project "<项目根目录>"   # 先看清单：缺什么、将生成什么
python scripts/init-workspace.py --check --project "<项目根目录>"  # 只体检：五类产物在不在 ＋ 通道侧只读探测（都不改文件）
python scripts/init-workspace.py --yes --project "<项目根目录>"    # 生成（不覆盖已存在的非空文件）
python scripts/init-workspace.py --clean-tmp --project "<项目根目录>"  # 清空 .workbuddy/tmp/（每轮收尾跑）
```

**`--check` 顺带只读探一次通道侧**（2026-09-23 补）：按「`--cli` 指定 ＞ 环境变量 `QQBROWSER_SKILL` ＞ 与解释器同目录 ＞ 该环境 pip 脚本目录 ＞ `PATH` 裸名」找 CLI，找到再跑一次 `--help` 取退出码。**探不到就报「通道未就位」并列出 §1.0 ① 段的两个官方入口**；探到就报命中形态。

**为什么要探**：项目侧五项齐全、通道侧没装时，体检原先照样全绿——「没装通道」只能靠人读到 §1.0、或第一次跑命令报错才浮现（本机实测：体检绿着通过，第一轮任务卡在命令找不到）。**三条边界**：① **只读**——不拉起浏览器（守 §3.0.3）、不安装任何东西；② **②③ 段不代查**——浏览器在不在运行、扩展有没有接入，须由使用者在自己的桌面会话确认；③ **不计入项目侧五项、也不改退出码**——它是 ① 段的提前预警，不是项目侧的第六项。

**生成的五样东西**：

| 产物 | 落点 | 来源 | 性质 |
|---|---|---|---|
| 项目级要求文件 | **项目根** `CODEBUDDY.md` | `templates/CODEBUDDY.md`（占位符按 §1.1 替换） | 生成后**由使用者核对** |
| 台账 | 配置项 6 路径 | **手册附录 A 模板块**（脚本就地抽取，不另存副本） | 初态即绿 |
| 变更档案 | 配置项 6 路径 | **手册附录 B 模板块**（同上） | 初态即绿 |
| **投递台账**（**xlsx**） | **信息目录**（＝数据层所在目录，默认 `<项目根>/info/`） | **附录 C 的列结构**（脚本内建生成，不抽取文本模板块） | 初态即绿 |
| 数据层（**一份单文件**，即配置项 4 点名的那个） | 配置项 4 路径（其所在目录即**信息目录**） | 脚本内建**最小合法骨架**（Markdown，一份兼两职） | **内容须使用者填** |

**同时建好的目录**（2026-09-23 补）：① **信息目录**（＝数据层所在目录）——数据层、投递台账、其余信息文件与附件原件同置此处，**项目根只留要求文件与启动器**；② **三类运行目录**——`.workbuddy/tmp/`（临时产物，**每轮收尾清空**）、`.workbuddy/evidence/`（过程证据，保留）、`.workbuddy/backup/`（改前留档，保留）。**为什么**：落点没有目录归属时，探查脚本与回读输出会散在数据层旁边——它们常含手机号、证件号、成绩，既污染「数据层＝该目录」的定义，也让清理时无人敢删（本机实测：一轮下来项目根堆了 154 个临时文件）；而「要清的」与「要留的」不分开，就只能一律不删（越堆越多）或一律删（丢证据）。

**三条硬纪律**（照搬 §3.0.1 启动器的做法，同一套道理）：

1. **占位符未替换即中止**——绝不静默产出一份带 `__XXX__` 的坏文件。`templates/CODEBUDDY.md` 里除"禁止触碰清单"允许留空外，任何 `__XXX__` 残留都判 FAIL 并中止。
2. **不覆盖已有内容**——目标文件已存在且非空即报 FAIL 跳过；要重建须显式加 `--force`。这与 §七 的"改前重读磁盘版"同源：多会话并行下静默覆盖＝丢他人改动。
3. **不编造个人事实**——数据层含使用者真实信息，脚本**只建结构、不填内容**：生成空值骨架并把"须使用者填"写在文件首行说明里。**绝不生成占位内容冒充数据**（§2.1 第 1 条"绝不编造"在初始化环节的落地）。

**为什么台账模板不另存一份**：模板的唯一真源是**手册附录 A／B／C 的 ` ```markdown ` 块**——脚本**在运行时就地抽取**（`extract_block` 按模板块标题定位，模板块挪到附录不影响抽取）。若另存 `templates/EXPERIENCE-LEDGER.md` 这类副本，就会出现"手册改了模板、副本没同步"的漂移，而这正是 §5.4 记过的事故类型（2026-09-17：模板与校验器判据不符，照模板建完的新环境必红）。**模板块放附录而不是拆成独立文件**，为的是同时拿到"正文不臃肿"与"只有一处真源"。

**生成后必须做的自证**（§5.4 要求"模板当输入跑一遍"）：

```bash
python scripts/verify-package.py --ledger "<刚生成的台账路径>"   # 期望：初态即绿、退出码 0
```

**引导使用者完成的清单**（脚本末屏会打印）：

1. 打开项目根 `CODEBUDDY.md`，核对 §〇.5 禁止触碰清单，以及 §〇.1／§一 的数据层**单文件路径**、投递台账路径、卷首效力顺序与 §五／§六 的通道文件路径——**这些脚本只能填占位，真值只有使用者知道**。信息目录默认取**数据层所在目录**，与脚本默认（`<项目根>/info/`）不同时改这一处即可（或用 `--info-dir` 覆盖）。
2. 把简历事实与字段取值逐项填入数据层（配置项 4）——**这是取数的唯一真源**，不填则一切填写任务无从开始。
3. 按 §1.1 配置项 8 制备浏览器启动器——**优先用生成入口**`python scripts/init-workspace.py --make-launcher --project "<项目根>" --cli "<CLI 路径>" --browser "<浏览器 exe>" --url "<目标页 URL>"`（由模板产出、强制纯 ASCII）；也可手动套用 `scripts/launch-browser.bat` 模板。要点与**批处理编码硬规矩**见 §3.0.1。
4. **配方二选一**（§1.1 配置项 5）：默认**就地引用**包内 `recipes/`（多数环境已把它注册成技能，直接可用）；仅当包不在技能扫描路径内，才复制到环境侧配方目录。**同一环境同名配方只能存在一份**——否则会出现同名技能两份。
5. 跑一次 `python scripts/verify-package.py` 确认包自洽（退出码 0）。
6. 此后每次任务收尾，用同一支脚本带 `--ledger` 指向本环境台账跑**运行态**校验（第十段：轮次基准／待验表／到期／池量／登记簿对账）——生成的项目级要求文件 §七 里也是这么指的。

**已初始化环境重复跑会怎样**：`--check` 只体检、`--yes` 只补缺（已存在的非空文件跳过并报 FAIL 提示），**不会改动任何已有内容**。**注意「不必重跑」说的是初始化（`--yes`）**——它只在新环境、或补缺时才跑一次；**`--check`（开工体检）相反，每次任务开工都要先跑一次**（§4.1 第 0 步）——它只读、成本极低，而漏跑会让五项产物缺失一路静默到填写中途才暴露。**这条已写进项目级要求文件 §〇.7**（要求文件每次会话无条件注入，故不依赖本手册被加载）。

## 二、任务定义与硬性要求

**目标**：把数据层里的**真实**信息，准确填进各招聘系统的网申表单，并把过程沉淀成可复用配方与经验。

**四类成果物**：
1. **填好的表单**（页面状态或已保存）；
2. **投递台账新增一行**（填写完成且核对通过即追加）；
3. **配方更新**（新系统／新手法）；
4. **台账与变更档案更新**（本轮表现判定与条目变动）。

**硬边界**：不编造；不擅自投递；不擅自把单次取值固化成通用口径；**使用者当场指令优先**。

> §2.1–§2.2 为完整条文，不依赖外部文件。

### 2.1 行为红线（违反即事故）

1. **绝不编造**：没有事实依据的字段宁可留空或问使用者。必填但库里没有 → 问使用者；使用者不在就留白并明确告知"此项待你手动填"。**错填比漏填严重**。
2. **带星号（*）即必填**：直接对照数据源取数，不留空试探校验。
3. **简历附件绝不自行搜索**：PDF／成绩单／作品集版本更新频繁，必须问使用者要绝对路径。本地搜到的可能不是最新版。
4. **批量填表前先侦察**：动手前跑三步——判框架 → 选写入通道 → 扫字段清单（tag／必填星号／计数器上限／重复 DOM 树／动态增删字段），给出一句话计划再执行。**动态增删字段的表单一律按行 label 定位，禁用序号定位**（一次重渲染即全部错位）。
5. **不擅自投递 ＋ 投递台账**：任何「投递／提交／发送」按钮必须使用者明确指示后才可点击。台账记录生命周期（列：序号／投递日期／公司／岗位／投递渠道／状态／备注）：填写完成并回读核对后即追加一行，岗位未定写「待使用者补录」、状态用「暂存未提交」；正式投递后**问使用者**投了什么岗位再更新状态——只以使用者答复为准，不从页面推断。**骨架模板见附录 C**（新环境由 §1.5 一并生成）。
6. **填完必须回读核对**：保存后刷新／回读页面，逐项对照数据源检查，汇报"已核对 N 项，修正 M 项"。「上传简历自动解析」**只作脚手架**：解析会清空已填字段、把日期写回废弃口径、把无关内容填进错误区块；用后必须整表回读、逐项修复并把长文本换回定稿。日期类回读必须核对到「日」，控件只到月时按当月口径记录。**核对必须双向**：除「表→库」逐字段比对外，还要做「库→表」覆盖清点——按库内条目清单（实习／校园／项目／奖项／家庭／证书／语言等）逐桶确认是否已在表中落位；表单无对应区块时按经历归类口径就近归位或问使用者，**不得默认丢弃**。
7. **保存成功判据**：以整页刷新后数据仍在为准，表单打开不等于保存成功；单条目操作一气呵成，保存按钮**只点一次**（双击会触发重复提交弄掉会话）。多区块（教育／实习经历等）**逐条目即时保存**，页面丢失或黑屏时损失以单条为限。
8. **先填后问**：已有完整信息的字段／区块全部先填并保存；缺数据的跳过并记录（该区块因必填缺失无法保存时：表单保持打开，不点取消、不刷新页面）；全部可填项处理完后，把缺失数据**一次性汇总询问**，得到答复后回填补存。不为个别字段阻塞整体进度，也不边填边频繁打断使用者。
9. **不维护进度 md**：不创建、不更新进度记录类 md 文件；填报进度在对话里直接汇报，台账记录按 §2.1.5。
10. **整块完成才刷新、打断不续跑**：验证性刷新按「整块任务」粒度做（一轮填写完成后刷新一次），不在条目之间反复刷新——与第 7 条"逐条目即时保存"不冲突（保存每条都做，刷新整块一次）。使用者中途打断的任务**不自动恢复续跑**，等使用者明确指示。

### 2.2 内容填报口径（通用原则随包，专属取值不随包）

**本节不内置任何个人化内容**——“某个字段该写成什么样”属使用者专属口径，由**使用者数据层**（配置项 4，如字段映射／口径说明文件）提供。下列五条与具体系统无关的**通用原则随包提供**；数据层缺失时按它执行：

1. **只填字段明确要求的内容**：未要求成绩就只列课程名、未要求描述就不写描述、未要求时间就不写时间，不附加未要求的信息。
2. **一律取数据层原文、不自行改写**：定稿类文本（自我介绍、自我评价、项目描述等）直接取用，不改写、不润色、不缩写。
3. **宁宽勿假**：分档类字段（成绩排名、掌握程度、等级类）只能选**真实涵盖真实值**的档位；档位不覆盖真实值时改选兜底项或留空（非必填），**绝不上靠**。
4. **同类合并、不重复罗列**：自由文本栏内的同类项合并成一条；同一内容已在别处落位的不重复填。
5. **写完逐项核对内容与标签是否对应**：发现错位或错填当场改正，再保存。

## 三、通道规范：QQ 浏览器

> 本章为该通道完整口径。**其他通道的操作经验不可直接套用**——差异点见 §1.3。

### 3.0 通道可用性前提（先看这条）

两条前提**必须同时成立**，否则命令会以 60 秒超时告终——**「浏览器开着」≠「通道可用」**。

- **前提一：浏览器必须由使用者在自己的桌面会话中启动并保持**。通道 CLI 假设浏览器是长期存活的独立进程；若执行环境会在每条命令结束时回收该命令拉起的子进程，自行拉起的浏览器就活不过当前命令（使用者会话下的进程不受该回收影响）。
- **前提二：浏览器必须带内置扩展启动**。启动时缺扩展加载参数，内置扩展不加载，通道侧会空转 60 秒后报超时。**参数清单属环境配置**，按 §1.1 配置项 8 填一次；使用者侧可备一个「双击即用」的启动器。

**可用性判据**：命令返回 `success`（而非 60 秒超时）；或通道日志出现「客户端已确认连接」。

**症状对照**：命令恒报「浏览器未启动，尝试启动」、会话标签组查不到、读取落到浏览器首页而非目标页、命令一律 60 秒超时——**先怀疑本条，而不是 CLI 坏了**；**但 `命令找不到`／提示未安装不属本条**，那是 CLI 没就位，走 §1.0 第 ① 段；若是**跑脚本时** `python` 找不到，走 §1.0 脚本侧。接续任务前先确认浏览器在运行；不在就停下来请使用者启动，**不要自行拉起浏览器**。

#### 3.0.1 使用者侧启动器（建议随环境制备一份，双击即用）

一条龙三段：`taskkill` 旧实例 → 起常驻 daemon → 带扩展参数开浏览器并打开目标 URL；末尾把进程／端口／连接数写进固定日志。要点：

- **必须先关旧实例**：浏览器单实例，已开着时再启动，**新参数会被吞掉**——表现为进程命令行仍是旧启动方式（如 `-sc=desktopshortcut`）、扩展不加载。
- **扩展加载参数是必需开关**，不是可选优化；缺它命令必然 60 秒超时。
- **制备方式：优先用生成入口，不要手写**。`python scripts/init-workspace.py --make-launcher --project "<项目根>" --cli "<CLI 路径>" --browser "<浏览器 exe>" --url "<目标页 URL>"`（§1.5 步骤三）由本封装体模板直接产出——生成器会把取值与产物**双双强制为纯 ASCII**，手写最容易在这一步翻车。也可手动套用 `scripts/launch-browser.bat` 模板：把 4 个 `__占位符__` 换成 §1.1 配置项 8 的取值，**未替换就运行会在启动前中止并打印提示**（不会静默把日志写进名为 `__LOG__` 的文件）。
- **注释与全部文本一律纯 ASCII（硬性）**。`.bat` 由 `cmd.exe` 按**本地 ANSI 代码页**（中文 Windows 即 GBK）解析；文件里若写了 **UTF-8 的中文**，注释行会被撕成碎片并**当命令执行**（2026-09-24 实测：报错形如 `'紙--display-invisible-extension' 不是内部或外部命令`，而当时**九段全绿**——原判据只看「能不能解开」，不看编码）。**别靠「存成 GBK」绕过**：任何一次用 UTF-8 重存即复发。**规矩是纯 ASCII**，启动器与一切批处理都适用；包内 `*.bat` 含非 ASCII 现直接判 FAIL（§6.3 第五段）。
- 日志落在固定路径（如 `.workbuddy/tmp/launcher.log`）：出现 **`Connected clients: 1`** ＋端口 `ESTABLISHED` 即为扩展已接入。**接续任务前先看这一行**，别急着下页面命令。
- 收尾停在 `Press any key to continue` 的窗口**不要关**——daemon 依赖它存活。

#### 3.0.2 daemon：唯一稳定的起法

| 起法 | 结果 |
|---|---|
| 默认（子命令隐式拉起） | **daemon 与命令同生共死**——每条命令现场起、命令结束即销毁；副作用：会话标签簿记随之丢失 |
| `serve --daemon` | 进程能活 60s+，但**不 bind 端口**（`netstat` 查不到 8765/8766），CLI 认不到它、于是另起一个 |
| **`serve --daemon --call-mode qbotclaw`** | ✅ **唯一稳定常驻**：跳过浏览器检查、auto-stop 既有 daemon、真正 bind 端口、pid 不漂移 |

- `--call-mode` 的语义是「由外部调用者启动」，**它跳过浏览器检查**。不跳过时，daemon 每次都会跑「浏览器是否在运行」的检查，判定「未启动」就**新拉一个浏览器实例并打开首页**，导致标签页与登录态反复被洗。
- **判活不要用 `status`**：它读的是 spawn 壳的 pid，与真实 daemon 不同步，**服务正常时也会报 `❌ Daemon is not running`**。能否用只看**命令是否返回 `success`**。
- `serve` 自身会先关掉既有浏览器（升级／重启流程同理）——**跑 `serve` 前先确认无未保存页面**。

#### 3.0.3 不要自行拉起浏览器（已排除的手段，别再试）

在本环境「自行拉起浏览器」**不可能成功**：命令结束时会回收该命令产生的整个进程树。已实测排除：`subprocess.Popen` 普通启动、`DETACHED_PROCESS`、`CREATE_NEW_PROCESS_GROUP`、PowerShell `Start-Process`（四种皆表现为「同一命令内可见数十进程、下一条命令归零」）。借系统服务派生的路径（COM 建快捷方式、WMI／计划任务创建进程）**被安全策略拦截**——属有意的隔离设计，**不要绕**。

**结论：浏览器由使用者启动是分工，不是权宜之计。**（例外只有一处：见 §3.0.4 的收尾动作）

#### 3.0.4 升级陷阱（升级前必读）

通道 CLI 的自动升级流程是**破坏性动作先成功、修复性动作必败**：

1. 停止既有 daemon（通常「not running, skipping」）
2. `taskkill /F /IM <浏览器>.exe` —— **成功，浏览器被杀**
3. 重装浏览器 —— **失败**：升级器用**裸命令名** `qqbrowser-skill install` 调子进程，而该命令**不在 PATH**（须用绝对路径），抛 `WinError 2`（系统找不到指定的文件）

**后果**：浏览器被杀且未恢复，且**不会自愈**——`install` 的检测只看**可执行文件是否存在**（文件一直在），恒判「已安装」，永不触发修复。症状与 §3.0 两条前提相同（命令 60 秒超时、扩展不连接）。

**处置**：请使用者用 §3.0.1 启动器重开浏览器。**预防**：升级前先请使用者存好页面；升级后**主动检查浏览器是否还在**，不在就立刻请其启动，不要等命令超时才反推。

### 3.1 通道标识与会话模型

- **驱动方式**：`qqbrowser-skill` CLI（形如 `qqbrowser-skill browser_snapshot --sessionId <id> --markdown`），接管 QQ 浏览器。**不是** CDP proxy 类通道。
- **隔离模型**：每个任务 `browser_start_session --sessionId <id>` 开头，`browser_end_session --sessionId <同一个 id>` 收尾（成功、失败、中断都要收）。会话内操作落在**独立 Chrome 标签组**，与使用者自己的标签页天然隔离。
- **`sessionId` 由 `get_session_id` 铸造，不自己编**：返回 `T-<10 位随机数字>-<1 位 Luhn 校验位>`（如 `T-8492173046-9`），是不透明 token，逐字符抄（含连字符）。**一段对话只铸造一次、整段复用**；只有新对话才拿新 ID。旧文档里 `task-<用途>-<序号>` 的写法已作废。
- **会话幂等**：同 `sessionId` 重复 start 复用同一标签组；end 只释放内存映射、标签组保留给使用者查看。WebSocket 断连约 30 分钟后扩展自动收会话，但不作为正常退出路径。
- **复合任务只用一个 sessionId**：子任务之间做 AI 中转（整理、改写），不重开会话。
- **会话强绑定（CLI 1.5.9 起）**：页面级交互／读取命令**必须显式带 `--sessionId`**——漏带时 **CLI argparse 直接拒**（`error: the following arguments are required: --sessionId`），CLI 已不再隐式注入当前会话。**哪些命令需要、哪些豁免，见 §3.2 每行的 `[sid]` 标记。**
- **会话绑定来源＝`browser_start_session`**：只有它真正建立标签组——返回体含 `groupId`／`tabId`／`mode:"enforce"` 才算成。`get_session_id` 只是铸 id、`tab_open` 只是开标签，**都不建组**。
- **`get_session_tab_list` 恒返回 `[]`，不能作判据**：实测即使会话组已建成（`mode:"enforce"`）且页面级命令精确落在组内标签上，它仍返回空。想确认"命令会落到哪个页面"，用 §3.6 的**落点核对**，不要用这张列表。
- **并发**：多个 sessionId 可同时存在（一个 id 一个标签组，互不可见），但**扩展侧有全局执行器锁**，跨会话命令实际排队（诊断字段 `holder/holdingMs/waiting`，见 `browser_session_guard_query`），等待超时报 `session_busy_timeout`——并发是"多个任务同时挂着"，**不是两倍速度**。并发下每条命令都要带对 id，带错即把命令下到别的会话的页面上。

### 3.2 命令速查

（`[sid]` ＝ 必须显式带 `--sessionId <id>`；**表中与全文的 `qqbrowser-skill` 一律指可解析的调用形式**——裸命令名在多数环境不在 `PATH`，见 §1.0 第 ① 段与 §3.0.4；**`python` 同样是占位名**，见 §1.0 脚本侧）

| 用途 | 命令 |
|---|---|
| 会话 | `get_session_id`（铸造，一段对话一次）、`browser_start_session --sessionId <id> [--title "<t>"] [--initialUrl <url>]`、`browser_end_session --sessionId <id>`、`browser_attach_session [sid]`（**禁用**：卡死并堵住队列，见 §3.4） |
| 导航 | `browser_go_to_url [sid] --url <url>`、`browser_go_back [sid]`、`browser_wait [sid] --seconds <n>` |
| 取元素索引 | `browser_snapshot [sid]`（交互用，带索引）／`--markdown`（只读内容） |
| 语义定位直操作 | `browser_find_and_act [sid] --by <role\|text\|label\|placeholder\|alt\|title\|testid\|css\|first\|last\|nth> --value "<v>" --action <click\|fill\|type\|hover\|focus\|check\|uncheck\|text> [--actionValue "<v>"] [--nth <n>] [--exact\|--no-exact]` |
| 点击／输入／聚焦 | `browser_click_element [sid] --index <id>`、`browser_dblclick_element [sid] --index <id>`、`browser_focus_element [sid] --index <id>`、`browser_input_text [sid] --index <id> --text "<v>"` |
| 下拉／勾选 | `browser_get_dropdown_options [sid] --index <id>`、`browser_select_dropdown_option [sid] --index <id> --text "<v>"`、`browser_check_op [sid] --index <id> --value/--no-value` |
| 键盘 | `browser_keypress [sid] --key <k>`、`browser_keyboard_op [sid] --action <type\|inserttext> --text "<v>"`、`browser_keydown [sid] --key <k>`、`browser_keyup [sid] --key <k>` |
| 读状态 | `browser_get_info [sid] --type <text\|url\|title\|html\|value\|attr\|count\|box\|styles\|list_selector>`、`browser_check_state [sid] --state <visible\|enabled\|checked> --index <id>` |
| 页面内 JS | `browser_eval_content_js [sid] --script "<js>"`（长脚本可 `--base64`） |
| 页面读取（只读） | `browser_get_tab_content`（**不带 sessionId**，不激活标签、不切页）、`browser_markdownify [sid]`、`browser_screenshot [sid]` |
| 滚动 | `browser_scroll_down [sid]`／`browser_scroll_up [sid]`、`browser_scroll_by [sid] --amount <n>`、`browser_scroll_to_text [sid]`、`browser_scroll_into_view [sid]`、`browser_scroll_to_top [sid]`／`browser_scroll_to_bottom [sid]` |
| 任务收尾信号 | `browser_done`（`--success` 或 `--no-success` 二选一 ＋ `--text "<t>"`）——CLI 能力表 52 项之一，**本通道未实测其作用与必要性**（收尾现只用 `browser_end_session`），见 §3.5 |
| 标签页 | `browser_tab_open [sid 可选] --url <url>`、`browser_tab_list`、`browser_tab_switch [sid 可选] --tabId <n>`、`browser_tab_close`（**须带 `--tabId`；能关掉使用者自己的标签页**，见 §3.4）、`browser_tab_reload`（**空操作**，见 §3.4）、`get_session_tab_list --sessionId <id>`（**恒返回 `[]`，不可作判据**，见 §3.1）、`browser_tabgroup_*`（list／get／create／update／close／add_tabs／remove_tabs／move，均不带 sessionId） |
| 下载／弹窗 | `browser_download_file [sid] --index <id>`、`browser_download_url [sid] --url <u>`、`browser_dialog [sid] --action <accept\|dismiss>` |
| 排查 | `browser_session_guard_query`（只读：globalMode／activeSessionIds／执行器锁）、`debug`（收集调试信息与日志为 tar.gz，排错取证用） |

### 3.3 定位三档（按顺序，别跳档）

1. **语义 locator 优先**：`browser_find_and_act`。`--by` 可取 `role｜text｜label｜placeholder｜alt｜title｜testid｜css｜first｜last｜nth`；`--action` 可取 `click｜fill｜type｜hover｜focus｜check｜uncheck｜text`。"按行 label 定位控件"走这一档；同类多实例用 `--nth N`（1-based）。
2. **snapshot 编码索引**：语义 locator 不适用时（自绘组件、图标按钮、无文本按钮）才用。索引形如 `2_sfli_qp0u`，**每次 snapshot 重建**——不复用旧索引、不自己编数字索引；DOM 一变（提交、弹窗、AJAX）必须重新 snapshot。
3. **页面内 JS 兜底**：批量读写、DOM 事件派发、组件实例直连，统一走 `browser_eval_content_js`。

> **配方里的「真实点击／测坐标」在本通道怎么落地**：本通道**没有坐标点击命令**（子命令表无 mouse／coord 类）。配方写"测元素 rect 中心再点"的步骤，一律换算为——先 `browser_snapshot` 取索引 → `browser_click_element --index <id>`；能用文本／CSS 定位的直接 `find_and_act --by <css|text> --value "<v>" --action click`；面板开层后点选项用 `find_and_act --by text --value "<选项文本>" --action click`（§3.4 实测有效路径）。

### 3.4 能力边界（一手实测，不是推测）

| 事项 | 结论 | 依据 |
|---|---|---|
| 上传文件 | **无上传命令**——扩展侧白名单含 `upload_file`，但 CLI 未暴露为子命令 | `--help` 子命令表；包内 `x5use/skill_registry.py` 注释「集合中出现但本 CLI 尚未暴露为技能的能力（如 upload_file…）」 |
| 录制／回放 | **不存在**——`task_begin`／`task_end`／`task_latest`／`playbook_list`／`browser_replay` 均不在子命令表内 | `--help` 表；实测 `playbook_list`／`browser_replay` → `invalid choice` |
| `browser_attach_session` | **卡死**——发给扩展后 180 秒无回应（日志 `等待浏览器响应超时: action=attach_session_ack`），并**堵住扩展请求队列**，其后命令一并超时，须等它自行超时队列才恢复。**有已开着的标签要接管时，不要用它**：改为在会话组内 `tab_open` 打开同一 URL | 2026-09-16 实测两次 |
| `browser_tab_reload` | **空操作**——返回 `Success!` 但页面并未重新加载（`performance.now()` 纹丝不动）。**要真重载用 `browser_go_to_url` 重新导航**；验证保存结果必须用后者，否则会把"DOM 没被重置"误读成"落库成功" | 2026-09-16 实测（1,417,185 ms → 27,690 ms） |
| `browser_find_and_act --action type` | **卡死 >250 秒**（逐字符键入路径）。键入一律改用 `browser_input_text` | 2026-09-16 实测 |
| `find_and_act --action fill` | **扩展侧 DOM setter ＋ 合成 `input`／`change`**（`inputType` 为空串、**无** `beforeinput`、无键盘事件）；DOM 与 React props 都更新，但**凤凰／北森表单保存后归零** | 2026-09-17 北森 zhiye 正式表单（`<租户>.zhiye.com`）：埋事件监听对照 ＋ 保存后整页回读 |
| `browser_input_text` | **逐字符真实键入**（每字一次 `beforeinput(inputType=insertText)` ＋ `input`，带 focus/blur），中文无损、保存后落库；文本字段默认走它（§3.7.5） | 同上 |
| `find_and_act --action focus` ＋ `browser_keyboard_op --action inserttext` | **原子真实输入**（CDP `Input.insertText`），保存后落库；只需 CSS／文本定位 ＋ focus，**不必取 snapshot 索引** | 同上 |
| `browser_keyboard_op --action type` | 对自定义组件输入**不进值**（凤凰 select 的 input 实测写入无效） | 同上 |
| `browser_get_dropdown_options`／`browser_select_dropdown_option` | 只认**原生 `<select>`**；自定义下拉返回 `No options found in any frame for dropdown`，选中亦无效 | 同上 |
| `browser_click_element`（真实点击） | **能**打开凤凰日期面板（基线 0 → 点击后 `.phoenix-calendar` 1/1、body 子节点 +1）；但对凤凰下拉的 input／容器点击**打不开**面板 | 同上 |
| `find_and_act --by text --action click` | 是**真实点击**，能打开凤凰下拉面板；JS 合成点击、直接点 input、Enter／Space／双击**全部无效** | 同上 |
| `find_and_act --by label` | 只认**原生 `<label>`**（`for`／包裹）；标题是 `div` 的表单（北森等）**全部不命中** | 同上 |
| `find_and_act` 多实例匹配 | 默认取**第一个**；`--nth N` 为 **1-based 按 DOM 序**精确定位 | 同上 |
| `browser_click_element` 对按钮走 JS 回退 | 返回值写 `🖱️ Clicked via encodedId JS fallback`——**不是真实鼠标事件**。对自定义下拉的 input／容器点击打不开面板；**对"保存／暂存／提交"类按钮还可能「返回成功、服务端却没落库」**（页面无任何报错，只有另开标签重载才看得出）→ 这类按钮一律改用 `find_and_act --by text --value "<按钮文本>" --action click --exact` 真实点击，**返回值不作判据**；点一次即停，不连点 | 2026-09-16 点「暂存」曾生效；2026-09-24 在 zhiye 系岗位投递表单被证伪（JS 回退后另开标签重载读到的仍是旧值） |
| file input 上传 | `fill` 写本地绝对路径**无效**（`value` 空、`files.length=0`，浏览器安全模型禁止脚本写 file 控件）→ 本通道**不能上传** | 同上 |
| iframe 内元素 | snapshot 索引与语义文本定位**均能命中** iframe 内元素，`browser_input_text --index` **能写入**；但**点击不生效**（未完全跑通，兜底保留） | 2026-09-17 自建 iframe 页实测（`file://` 下 iframe 为跨源，读不到 `contentDocument`） |
| 页面弹对话框（`beforeunload` 等） | **其后命令整体失效**——`browser_eval_content_js` 直接返回失败并提示 `a dialog is currently open… Please use the "dialog" action to handle it first`；先 `browser_dialog --action accept`（或 `dismiss`）处理，同一求值命令即恢复正常。`--action` 取 `accept｜dismiss`，另有可选 `--text`（prompt 用）；**必须带 `--sessionId`**。**（2026-09-24 补）**表单页的 `beforeunload` 会**恒挂、且不反映脏态**——首次保存静默失败时照样弹、保存成功后照样弹，**不能当"有未保存改动"的判据**；此时「保存后回读」改为 `browser_tab_open --sessionId <id> --url <同一 URL>` **另开标签**重载读服务端数据，并用 `performance.timeOrigin` 分辨读到的是哪个标签（`performance.now()` 数字大的是留在身后的旧标签，其数据也是旧的） | 2026-09-19 实测：表单页用 `location.reload()` 做「保存后回读」刷新时被 `beforeunload` 拦下；2026-09-24 实测该对话框与本页是否真改动无关（含一次因读错标签而起的「没保存」误判） |
| 跨会话并发 | **全局串行排队**：一会话的长命令占住执行器时，另一会话命令等其完成才返回（实测等 5 秒）；本次**未触发** `session_busy_timeout`，阈值未测得 | 同上 |
| `browser_tab_close` | **必须带 `--tabId`**（argparse 必填，不带＝静默无效）；且**不受会话隔离限制，能关掉使用者自己的标签页**——只用来关自建中转页 | `--help` 原文；2026-09-17 实测 |
| 故障模型 | 命令返回错误码／文本，**进程不崩**；不做跨进程崩溃归因 | 实测 |

**上传处置**：本通道无上传命令，且 `find_and_act --action fill` 写本地绝对路径**实测无效**（`value` 空、`files.length=0`）——**本通道不能上传**，一律请使用者在浏览器里手动上传，**不换通道顶替**。附件路径一律问使用者要。

### 3.5 未验能力与兜底动作

未验能力一律**不先做实验**，当场按兜底动作走；验到结论就**就地回写**（§5.3 第 5 条）：事实补进 §3.4、引用处摘掉【未复现】。

**当前待验 1 项**：`browser_done`（CLI `list` 能力表 52 项之一，见 §3.2）是否**必调**、不调是否影响扩展侧任务态与会话回收——**未实测**；因涉及改既有收尾流程，先不动既有做法，遇一次任务收尾顺手观察即可。
（旧待验项「30 分钟断连后接回」已于 2026-09-17 用户裁定删除：用不上，中断后一律 `get_session_id` 起新会话，不赌接回。）iframe 点击路径与 `session_busy_timeout` 阈值的兜底口径见 §3.4 对应行。

> 本节与 §3.2／§3.4 均以 CLI **1.6.0** 实测为准（2026-09-16 逐条核 `--sessionId` 必须性）。换版本时这三处要一起重核——不是只补一处。

### 3.6 页面安全

标签组隔离是通道给的保障，**不是"可以随便操作页面"的许可**。

1. **只动本任务的目标页**：动手前先读当前页面 URL／标题，与任务目标页逐字核对，不一致就报告、不操作。使用者自己的标签页一律不碰——不点击、不滚动、不提交、不改 DOM。
2. **不 navigate 顶替**：找不到目标页就问使用者，绝不自行 `browser_go_to_url` 猜地址，也不新开页面顶替使用者正在填的页面。
3. **自建页面用完即关**：确需另开页面（查资料、上传中转）时自己新建，任务结束关闭这些中转页；**任务目标页不关**（留在会话标签组供使用者查看）；使用者原有标签页一律保留。
4. **提交／保存类按钮一次点击一条记录**：不用 `--nth` 批量连点试探；任何「投递／提交／发送」按钮非使用者明确指示不点。
5. **页面异常先报告**：页面无响应、两次读取结果不一致、长时间空白＝页面异常——报告使用者并征得同意后再重载恢复，不在异常页上继续做功。

### 3.7 写入与脚本纪律

1. **`browser_eval_content_js` 一律整体 try/catch**：出错以 `'err:' + message` 字符串返回，绝不让异常逃出。需要多段等待就拆成多次调用，不在单次脚本里堆等待。
2. **命令报错不等于未执行**：先回读页面状态确认，不盲目重跑写入。
3. **定位不确定不用点击试探**：改用页面内 JS 派发 DOM 事件；尤其禁止对提交／保存类按钮反复试探点击。
4. **日期时间一律直接输入**：日期选择器优先程序化设值或往输入框写值＋触发事件；坐标点选只作兜底，且必须先按行 label 精确定位目标控件。
5. **文本字段默认走真实输入；例外按控件类型分流**：字段可键入（可见 `input`／`textarea`）时**默认走真实输入**（`browser_input_text`），不先试合成写入。判据是**失败模式的可观测性**——真实输入失败是**显性**的（值进不去，回读立刻发现）；合成写入失败是**隐性**的（DOM 与框架状态回读都有值，保存后整页回读才归零），等发现时可能已保存甚至已投递。**真实输入 ≠ 逐字符键入**，字段多不构成负担（「真实输入慢」不成立）。另有站点实测合成写入与真实输入**等效**（2026-09-16，Vue3 ＋ 自研 `md-` 组件库，见 `recipes/midea-autofill`），经「暂存→真导航重载→全新标签」三重复证落库——**合成写入并非普适失效**，但适用面比「能写值」窄得多，故仍不作默认。三类例外：
   - ① **隐藏／组件托管控件**（只读日期输入、单选的原生 input、复选组隐藏字段）：键入进不去，走**组件状态写入或键盘路径**（§3.7.6、配方日期节）——**不是"改用合成写入"**：对组件托管的 DOM 直写常只改显示、不入模型（el-date-picker 直写 input 不落库，写 Vue `$data` 才生效），只有**无框架托管的隐藏字段**（如 51job 那类）setter 直写才成立。
   - ② **远程联想字段**（学校／公司／专业）：走**下拉选择配方**——键入过滤仍是真实输入，**文本值由点选动作写入**；直接合成写文本只写显示值、不写关联 ID、不触发搜索。
   - ③ **本通道真实输入实测不可用**（`--action type` 卡死属**工具缺陷**，非真实输入本身的问题）：可降级用原型 setter 做草稿，但**保存回读发现缺失一律改真实输入重写**，并在汇报里点名缺失项。
   **无论走哪条，落库判据统一是「保存后整页回读」**（§2.1.7）。
6. **动手前先判框架、按框架选数据入口**：用一次 `browser_eval_content_js` 读判据——React（元素上存在 `__react*` 键）、Vue2（`__vue__`）、Vue3（`__vueParentComponent`）、Angular（`window.angular` ＋ `ng-model`），并统计字段容器数量（`.form-item`／`[class*=form-item]`／`[ng-model]`／`el-form-item`／`ant-form-item`）。**非文本控件（日期／下拉／单选／复选）一律优先找组件实例的数据入口直调**（文本字段按上一条走真实输入，不在此列）：React 沿 `fiber.return` 上溯业务组件的 `props.onChange`；Angular 取 `angular.element(el).scope()` 改 model 后 `$apply()`；Vue 取组件实例改 props／`emit`。只有**无框架托管**的原生表单／jQuery 字段才用 value setter ＋ `dispatchEvent('input','change')`。**不模拟鼠标交互**——自定义下拉的选项常在 body 级 portal 动态渲染，跨调用即关闭，点击必然反复失败。
7. **读值必须读框架内部状态**：读字段当前值一律读框架状态（React `fiber.memoizedProps.text/value`、Angular scope model），**不读 `input.value`**——否则会把"页面看着有值、框架里却是空的"假填误判为已填。下拉／单选的**候选项通常已在本地数据源**（组件 props／`datasource`／`ng-options` 绑定数组），按显示文本匹配直接取 value，不必展开 UI、不必请求接口。
8. **分批写入，每批 5–8 个字段**：单次 eval 写入字段过多易触发执行超时；超时不代表未执行，先回读确认再决定是否重做。
9. **回读核对与保存判据**按 §2.1.6／§2.1.7；**上传**按 §3.4「上传处置」；**验证性刷新**按 §2.1.10（整块完成才刷新）。
10. **分点编号的描述类字段一律用真实换行分隔**（`1.` `2.` `3.` 各占一行），**不用空格**——空格分隔会被部分系统在保存回读时吞掉、造成两点粘连；`textarea` 原生支持换行，写完保存后必须回读确认查看页按行显示。
11. **运行产物落点与收尾清理**：探查与批量写入产生的临时脚本、回读输出、日志一律落 `<项目根>/.workbuddy/tmp/`，**禁止落在信息目录或项目根**——那些文件常含手机号、证件号、成绩等个人数据。**三类运行目录各司其职**：`tmp/`＝临时产物（**要清**）、`evidence/<轮次标识>/`＝需留存的过程证据（页面截图等，保留）、`backup/<日期>/`＝改前留档（保留，改坏时能回滚）。**每轮收尾必须清空 `tmp/`**：跑 `python [封装体]/scripts/init-workspace.py --clean-tmp --project "<项目根>"`（先印清单，只清 `tmp/`，**绝不触碰 `evidence/` 与 `backup/`**）——**不留到下轮**：堆积会让本轮脚本与历史回读混在一起，既污染下一轮的上下文，也让含个人数据的文件长期滞留（本机实测：一轮下来项目根堆了 154 个临时文件，散在数据层旁边）。已散落在别处的按此归位（**只移不删**）。

## 四、流程与分层

### 4.1 开工

**第 0 步 · 部署体检（漏了它，后面全白做）**：先跑 `python [封装体]/scripts/init-workspace.py --check --record-trace --project "<项目根>"`，五项必检产物（项目级要求文件／数据层单文件／**投递台账**／台账／变更档案）有一项缺失就先走 §1.5 补齐，**再开工**。 同一支脚本顺带看**目录结构**：信息目录（数据层所在目录）与 `.workbuddy/` 下 `tmp/`／`evidence/`／`backup/` 三类运行目录缺失时一并提示——它们缺了同样不报错，只会让临时产物重新散落在数据层旁边。**为什么要有这一步**：§1.5 的产物都不随包，缺了**不会报错**、只会静默降级——本机实测的代价是：本项目一直没跑初始化，直到使用者来问"投递台账做了吗"才发现（手册 §1.5 对流程规定完整，但"开工时不查"就永远轮不到它生效）。体检**只读、不改任何文件**（`--record-trace` 只向 `.workbuddy/evidence/deploy-check.log` **追加**一行留痕，不算改文件），随时可跑；它同时**只读报一次通道侧**（CLI 能否解析／能否打印用法）——① 段不提前过，这里五项全绿也开不了工，故先看体检的通道侧那一块再动手。**本条已同步进项目级要求文件 §〇.7**——要求文件每次会话**无条件注入**，故不必依赖本手册被加载（§1.5 记过的事故根因正是「手册规定完整、模型却未读」）。**留痕即判据（2026-09-24 补）**：带上 `--record-trace`，留痕文件由机械校验第十段查——缺失即 FAIL、末次留痕早于 7 天报 WARN；「体检跑没跑」从此有痕迹可查，不靠人记。

1. **取会话**：先确认浏览器在运行、扩展已连上（§3.0；不在就请使用者启动，不自行拉起）→ `get_session_id` → `browser_start_session`。一段对话一次、整段复用；其后所有页面级命令显式带 `--sessionId`。
2. **认数据层**：确认要填的信息以配置项 4 为准；快照／记忆／上一轮读数都不算。
3. **核对目标页**：读 URL／标题，确认就是使用者指定的那一页；不自行导航顶替。
4. **给一句话计划**（填哪些区块、预计几次写入、哪些必须先问使用者），再动手。

### 4.2 侦察（不侦察不动手）

1. **判框架**：按 §3.7.6 读判据，并统计字段容器数量。
2. **识别系统**：对照 §4.7 的判据对照表（URL、资源域、类名前缀、组件库指纹），确定框架归属与专属配方。
3. **扫字段清单**：tag／必填星号／计数器上限／重复 DOM 树／动态增删字段——动态增删字段一律**按行 label 定位，禁用序号定位**。

### 4.3 取配方（三层，按顺序）

前置层＝本手册；框架层／系统层＝**配方**（生效的那一处：默认包内 `recipes/<名>-autofill/SKILL.md`，复制路线为 `<配方根目录>/<名>-autofill/SKILL.md`；索引见 §4.7）；数据层＝配置项 4。

配方以**文件**形式使用：直接用 Read 读正文，**不依赖技能注册表**——包内 `recipes/` 是二级目录、环境侧配方目录是一级目录，两者**是否被所在环境扫描成技能都不影响用法**（各环境对目录的扫描规则本就不一致），别指望靠技能注册表取配方。只有读文件这一条路。**无配方时按框架层手法执行，收尾时在 §1.1 配置项 5 选定的那一处新建对应文件（§5.2），再按 §1.4 单向同步进包。**默认路线（未复制）下，包内 `recipes/` 就是有效来源**——项目侧配方目录为空不等于「没有配方」（本机实测：包内 12 份配方已被技能扫描器注册成技能、可直接读）。两条路线只走一条，见 §1.1 配置项 5。

### 4.4 写入（写入与回读配对）

- **一区块一次脚本**批量设值（5–8 个字段一批、脚本整体 try/catch），**写完立即回读**——读**框架状态**，不读 `input.value`。
- **文本字段默认走真实输入**（`browser_input_text`）；**例外三类**（隐藏／组件托管控件走组件状态写入或键盘路径、远程联想字段走下拉选择配方、本通道真实输入不可用时降级）见 §3.7.5。
- 值被 re-render 还原 → 改用真实键入重写再回读；下拉／单选候选项在本地数据源按文本匹配取 value，不展开 UI。
- 保存类按钮一次点击一条记录；**整块完成后**才做一次验证性刷新。
- 定位、日期、上传与红线的细则见 §3.4／§3.7／§2.1。

### 4.5 收尾核对

1. **库→表覆盖清点**：数据层每一条都要在表上有落点，或明确报告使用者（就近归位／报告，不默认丢弃）。
2. **全局回读**：必填零空缺；被静默还原或清空的字段补回；日期类核对到「日」。
3. **投递台账**：核对通过后追加一行；岗位未定写「待使用者补录」、状态「暂存未提交」；正式投递后**问使用者**投了什么岗位再更新，只以使用者答复为准（§2.1.5）。
4. 跑**优化闭环**（§五）。

### 4.6 分层与归属（维护用）

判据：**适用面越广越往上，不看发现地**。

| 层 | 管什么 | 判定问题 | 落点 |
|---|---|---|---|
| 要求 | 必须做／禁止做 | 违反了会出事故吗？（红线） | 本手册 §二（使用者另有要求文件时以它为准） |
| 通道 | 本环境怎么驱动浏览器 | 换浏览器／换机器还成立吗？ | 本手册 §三 |
| 配方 | 某类框架、某个系统怎么做 | 只在这一个系统／这一种框架能用吗？ | **配方（生效的那一处）**（框架层／系统层；二选一见 §1.1 配置项 5） |
| 数据 | 某个字段填什么 | 只是"取值"吗？ | 使用者数据层（字段映射／口径文件） |
| 证据 | 为什么保留／修订／删除 | 只用于判定，不产生要求 | 台账与变更档案（配置项 6） |

**数据流**：数据层 → 字段映射（取值与口径）→ 页面写入（经配方手法）→ 回读核对 → 投递台账。

**两条不可交换的边界**：
1. **通道命令不写进配方与数据层**——配方只写"做什么、判据是什么"，命令形式由通道层换算；否则换通道即全错。
2. **取值不写进配方与要求**——某系统某字段填什么属数据层，写进通用配方必随系统演进而漂移。

### 4.7 系统判据对照与配方索引

**判据对照（侦察时用）**：

| 判据 | 框架归属 |
|---|---|
| `zhiye.com`，有 `.phoenix-select` | 北森 Phoenix（React，fiber `props.onChange` 直调）→ `recipes/beisen-zhiye-autofill` |
| URL 含 `resumeOperation.html`、字段容器 `.ant-form-item`、区块容器 `.resume-content-wrap`、元素带 `__reactFiber$` | 北森 Wecruit（React16 ＋ antd3；与 zhiye 系 Phoenix 是两套，**不可互换**）→ `recipes/wecruit-autofill` |
| 服务端渲染 Bootstrap 风格，`jQuery` 在、`window.Vue === false`、无 React fiber；区块是各自独立的 URL 页（`/resume/xxxList`）而非同页 Tab | 服务端渲染纯表单（DOM 直写有效）→ `recipes/chnenergy-autofill` |
| 页面有 `ng-app`／`ng-model`，控件用自研 `hc-*` 组件 | AngularJS（scope model 直改 ＋ `$apply()`） |
| URL 含 `/TGnewUI/`、页脚有 Infinite Talent／brassring | 服务端渲染纯表单（DOM 直写有效） |
| `.el-form-item`／`.el-select`／`.el-date-editor`，元素带 `__vue__` | element-ui（Vue2）系 → `recipes/webform-autofill` |
| `.ant-form-item`／`.ant-select`／`.ant-calendar-input`／类名前缀 `aply-*` | antd ＋ React（16/17）系 → `recipes/antd-webform-autofill` |
| `el-select-tags-wrapper`／`el-input__wrapper`／元素有 `__vue_app__` | Vue3 ＋ Element Plus |
| 有 `.ud-formily-item`／`.ud__select`／`.ud__input` | React ＋ Formily ＋ UD 设计系统 |
| `careers.midea.com`，区块类名前缀 `ihr_recruit_resume-block-*`，控件 `md-input__inner`／`ihr_base_picker`（Vue3，元素带 `__vueParentComponent`） | Vue3 ＋ 自研 `md-` 组件库 → `recipes/midea-autofill` |
| `app.mokahr.com`，类名前缀 `sd-Input-*`／`sd-Select-*`／`sd-Tabs-*` | React 16 ＋ Moka 自研 sd 组件库 → `recipes/moka-autofill` |
| `body` 类含 `saas-career`、资源域含 `feishucdn.com/obj/atsx-throne`、字段容器 `.ud-formily-item`、控件 `.atsx-upload` | 飞书招聘 ATSx（React ＋ Formily ＋ UD） |

**归属判据**：下次遇到同类系统还能用 → 框架层；只在这一个系统能用 → 系统层；只是取值 → 数据层。系统专属判断点 ≥5 条且不跨系统复用时才单独立文件。

**配方索引**（下表路径按包内写；若已复制到环境侧，对应 `<配方根目录>/<名>-autofill/SKILL.md`；两处同名同构，**只改生效的那一处**）：

| 配方 | 层 | 覆盖 | 典型系统 |
|---|---|---|---|
| `webform-autofill` | 框架 | element-ui（Vue）系 | 智联校招等 |
| `antd-webform-autofill` | 框架 | antd（React 16/17）系 | `applyjob.chinahr.com` 等 antd 门户 |
| `zhaopin-autofill` | 系统 | 智联招聘 | `xiaoyuan.zhaopin.com` |
| `nowcoder-autofill` | 系统 | 牛客网申 | `nowcoder.com` |
| `antgroup-autofill` | 系统 | antd 系校招门户专属实现 | `talent.antgroup.com` |
| `beisen-zhiye-autofill` | 系统 | 北森 Phoenix | `<租户>.zhiye.com` |
| `wecruit-autofill` | 系统 | 北森 Wecruit（React16 ＋ antd3；与 zhiye 系是两套，不可互换） | `wecruit.hotjob.cn` |
| `51job-autofill` | 系统 | 前程无忧／应届生 | `xyz.51job.com` |
| `feishu-atsx-autofill` | 系统 | 飞书招聘 ATSx（含 `scripts/`） | 各租户 SaaS 门户 |
| `midea-autofill` | 系统 | 美的校招官网（Vue3 ＋ 自研 `md-` 组件库） | `careers.midea.com` |
| `moka-autofill` | 系统 | Moka 校招候选人端（React 16 ＋ 自研 `sd-` 组件库） | `app.mokahr.com` |
| `chnenergy-autofill` | 系统 | 国家能源集团招聘系统（服务端渲染 Bootstrap＋jQuery，各区块独立 URL） | `zhaopin.chnenergy.com.cn` |

**新系统沉淀路径**：判据表未命中 → 按框架层手法执行 → 任务收尾在**生效的那一处**（默认包内 `recipes/`）下新建 `<系统名>-autofill/SKILL.md`（走复制路线时随后同步进包 `recipes/<系统名>-autofill/SKILL.md`；格式沿用现有文件：frontmatter `name`／`description` ＋ 识别判据 ＋ 专属手法 ＋ 字段清单）。判断点 <5 条或可跨系统复用时**不单独立文件**，并入框架层配方。

## 五、持续优化闭环

### 5.1 触发点与记录

**档位：默认轻量，可升完整**（2026-09-23 补）。本节机制分两档，**档位写在经验台账「计数与口径」段首的 `**档位**：轻量`／`**档位**：完整` 一行**，校验器按它决定查什么；**未声明即按完整档**（向后兼容旧台账）。

| 档 | 强制维护 | 不强制（可整节留空或不建） |
|---|---|---|
| **轻量（默认）** | 投递台账、轮次基准、每轮复核结论、事件流（含落点登记簿对账） | 待验池的域阈值／到期判定／池量上限／休眠区／折叠／漂移与固化率复算；轻量档下校验器**不要求待验表存在、不判到期与池量**，漂移／固化率只在已填数字与复算不符时才报错 |
| 完整 | 上列全部 ＋ 待验池（7 列）＋ 域阈值到期 ＋ 池量 ＋ 休眠区 ＋ 折叠 ＋ 漂移／固化率 | — |

**为什么默认轻量**：2026-09-23 新环境功能验证实测——治理机制自身在该环境**零运行证据**（台账两文件初态全空，阈值／休眠／折叠从未触发），而它要求使用者维护的节却最重；先把操作面跑通、等机制真有轮次可跑再升档才划算。**升档＝把台账那一行改成 `完整`，并按完整档补齐各节**（两档共用附录 A／B 的同一份骨架，完整档专用段在附录里已标注）。

**运行证据门槛（机制自己也被治理）**：**完整档**下，若连续 **3 个已复核轮**内事件流没有任何新增行 → 触发一次「机制是否过重」复核：要么降为轻量档，要么在该轮结论里写明为何仍需保留。校验器满足条件时给 WARN 提示，**降档与否由使用者定**；轻量档不受此约束。

**触发点**：每次任务收尾；或单次会话出现 ≥3 次「踩坑→修正」回路。

**两份文件，一行一条**：

| 文件 | 记什么 | 何时写 |
|---|---|---|
| 台账（当前态） | 【待验】条目（含「拟入层级」列）＋本轮复核结论（命中／证伪／未触发） | 每轮收尾 |
| 变更档案（登记簿＋事件流＋留档区） | 每条经验一个**条目 ID** 的登记簿（「类」／当前落点／状态，做分母）＋新增／修订／删除／降级／拆分／清出／回摆事件 ＋ 依据，＋抖动命中与每轮指标，＋留档区（已折叠事件／已删除·清出条目／休眠区） | 每次条目变动 |

台账文件按配置项 6 由使用者指定路径——记录的是**使用者的任务史**，**不随包走**；模板见**附录 A／B**，由 §1.5 的初始化脚本就地抽取；**投递台账是 xlsx**（二进制，无法用模板块承载），列结构见**附录 C**、由脚本内建生成。

**模板已移至附录**——附录 A（经验台账）／附录 B（变更档案）／附录 C（投递台账列结构）。两个模板合计 106 行，原先占本手册正文近五分之一，而它们是**要复制出去的文件骨架、不是要求正文**，放在这里只会让"正文有多长"这件事失焦。**为什么不拆成 `templates/` 下的独立文件**：§1.5 已论证"模板只留一处真源、脚本就地抽取"，拆成独立文件就会重新引入"手册改了、templates/ 没同步"的漂移；挪到同一份手册的附录，既压缩正文、又不动抽取路径。

### 5.2 分流与定层（封装体自治）

**前置约束：使用者的要求文件只读。** 要求文件一律**只读**（冲突时报使用者）；本闭环的落笔处是**生效的那一处配方、本手册、数据层与台账／变更档案**——就地引用时增补直接写回包内那一份，复制时写复制出来的那一份并单向同步进包（§1.1 配置项 5）。

1. **分流**：**取值类**（某字段填什么）→ 直接进数据层，单次即可、**不走门槛**；**手法类**（怎么操作控件／组件／系统）→ 首见即入台账【待验】并标注拟落点。
2. **门槛**：手法类需 ≥2 次不同任务复现，或一次**事故背书**（错填／数据被覆盖／会话丢失级别）→ 才上提入册；**事故背书免复现**。
3. **定层只按适用面判，不看发现地**：

| 经验类型 | 落点 | 写入权限 |
|---|---|---|
| 通用手法／红线／流程／失败模式 | 本手册 `SKILL.md` 对应节 | **§二 要求层须先获使用者批准**，其余可写（§5.4） |
| 单框架／单系统手法 | **生效的那一处配方** `<名>-autofill/SKILL.md`（无则新建；复制路线下随后同步进包） | 可写 |
| 取值类（某字段填什么） | 使用者数据层（配置项 4） | **先列单请使用者确认，再追加** |
| 证据（待验／本轮结论／事件流） | 台账与变更档案（配置项 6） | 可写 |
| 使用者的要求文件 | — | **只读**；冲突时以它为准并报使用者 |

4. **状态随行**：跨通道／跨系统搬来的结论，必须带**验证范围与确定度**（哪几个系统复现过、边界测没测、根因定没定）；**只搬结论不搬标注＝丢了一半信息**。
5. 上提后在该行备注写明落点，随即从【待验】表清出；不入册的**写明原因**。

### 5.3 回写顺序与验证

1. 先改**本手册**（若属通用要求），再改**生效的那一处配方**，最后改台账——避免台账先落、要求没跟上；配方改完按 §1.4 同步一次进包。**使用者的要求文件不在回写对象内**（只读，冲突走报备）。
2. 改完**逐处回读**：编辑工具报成功**不等于**落盘。
3. 批量改动后做一次残留检索；**检索必须给显式路径**——项目根检索会跳过隐藏目录。
4. **抖动与退役**：判据全文见 §5.1「抖动标记」段，此处只列触发条件与动作——按**条目 ID** 计：同一 ID 在最近 5 个已复核轮内变动 ≥3 次（【漂移】，**「类＝部件」的机制类 ID 只统计、不触发处置**），或删除／清出后又新增（【回摆】，单次即报），或**同一 ID 出现 ≥2 次证伪**（证伪单列强信号，与漂移同等处置、不必等到 3 次变动）→ 判为定义不清，只能**降级**或**拆分**；载体退役时全库检索引用并收口（改指新落点，或改「已退役、不再作为参考来源」），历史事件行移入留档区、不删。
5. **标记降级：把「未复现」逐渐清掉**（本机迭代的日常动作）
   - **判据与 §5.2 门槛不同**：配方里的【未复现】与 §3.5 的缺口，说的是**通道动作能不能用**这个一次性事实——**实测跑通 1 次即可降级**；§5.2 的「≥2 次复现或事故背书」管的是**手法值不值得入册**，别把这套门槛套到标记上。
   - **降级时三处一起改，顺序固定**：① §3.4 能力边界补一行（事实 ＋ 依据：命令、现象、CLI 版本）；② §3.5 删掉该行；③ 引用该缺口的配方把【未复现】改写成结论——`已在 <系统／页面> 复现（<日期>，CLI <版本>）`，**兜底写法保留**（换环境／换 CLI 版本仍可能失效）。
   - **只跑通一半就写一半**：结论本身带边界条件（如「能写入，但仅限同源 iframe」）时，标记**不摘**、兜底留着——边界没摸清不算复现。
   - **不要求一次清完**：按遇到的机会逐条降级，每条在台账记一行（§5.1 格式），【待验】表里对应行清出。
   - **本机改动不动 frontmatter 的 `version`**——那是源头版本标识；本机改了什么，记在自己的台账与变更档案里，日后换新版时靠这行认出「哪些是本机改的」。

### 5.4 本手册自身的迭代

**版本自证**：frontmatter 记 `version`（`v<主>.<次>`）与 `updated`（最后改动日期）——**当前版本号只在 frontmatter 一处**，本节不重复（两处写版本号必然不同步）。**维护者**每次实质改动递增**次版本**（本机自用改动不动 `version`，见 §5.3 第 5 条）；结构重排、整节增删、对外移交节点递增**主版本**（**"整节"＝§一–§六 的增删或重排；只增删 §X.Y 子节算次版本**）。历史**不写在本手册内**——一律进变更档案（配置项 6，事件流），每行注明版本号与依据。每次实质改动后跑一次包内机械校验（§6.3）的**封装体档**（`--package`，九段全跑），退出码非 0 不算改完；**每轮收尾**只跑默认的每轮档即可。**动过 §5.1 模板或 §6.3 校验脚本时，还须把模板当输入跑一遍**：照模板新建一份骨架（空节＋初态声明），用包内第十段与目标环境的校验脚本各过一遍，期望**初态即绿**。模板是包内唯一权威骨架，"正文里看着对"不算数——2026-09-17 实测：模板有两处写法与校验器判据不符（不计入范围形态、缺"冻结"），照模板建完的新环境必红，而两侧脚本从未把模板当输入测过。

**改动权限分级**（按"改错会不会出事故"分，不按工作量分）：

| 改动对象 | 权限 | 必要条件 |
|---|---|---|
| §二 任务定义与硬性要求（红线／口径通用原则／成果物定义） | **须先获使用者明确批准** | 未获批准只能记台账【待验】或当面报告，**不得直接改手册**——本手册自称使用者的唯一要求来源，单方面改要求层＝替使用者改要求 |
| §一（除 §1.1）／§三／§四／§五／§六（移交说明、通道事实、流程与闭环、自检与失败模式） | 可自行改 | ①有实测／复现依据 ②改后逐处回读 ③变更档案记一行 |
| §1.1 配置表 | 由部署者按环境填一次 | 只填取值；**改语义＝结构变更**，走上两行 |
| 使用者专属口径（某系统某字段填什么、使用者偏好） | **不入手册** → 使用者数据层（配置项 4） | 先列单请使用者确认再追加（§5.2） |

**与使用者要求文件冲突**：以使用者要求文件为准，使用者裁定后当场修正本手册（§1.4），并在变更档案该行注明"依使用者裁定"——手册内不留双份说法，裁定痕迹归档案。

## 六、自检与排错

### 6.1 收尾自检清单

- [ ] 数据层每一条都有落点，或已报告使用者
- [ ] 必填零空缺；写入值已回读，且读的是**框架状态**
- [ ] 投递／提交按钮未被擅自点击
- [ ] 投递台账已按实际情况追加／更新
- [ ] 台账两文件已更新：每条经验有表现判定，条目变动有事件行
- [ ] 本轮新经验已定层入册，或写明不入册原因（含验证范围与确定度）
- [ ] 本轮改动已回读磁盘确认；悬空引用已收口
- [ ] **本轮开工体检已跑且留痕在案**（开工第 0 步先跑 `--check --record-trace`，留痕落 `.workbuddy/evidence/deploy-check.log`）——收尾机械校验第十段会查它：缺失即 FAIL、超 7 天报 WARN（§4.1）
- [ ] **运行态机械校验已跑通**（`python scripts/verify-package.py --ledger <台账>`，退出码 0）——每轮收尾必跑；**改过封装体**（手册／配方／脚本／模板）再加跑 `--package` 档（§6.3）
- [ ] **`tmp/` 已清空**（跑 `init-workspace.py --clean-tmp`）；需留的证据在 `.workbuddy/evidence/`、改前留档在 `.workbuddy/backup/`；信息目录与项目根已无散落的探查脚本与回读输出（§3.7.11）

### 6.2 实测失败模式与陷阱

| 现象 | 处置 |
|---|---|
| 文件编辑/写入工具报成功，但磁盘内容未变 | **改完必须回读磁盘**；批量改完再做一次全量检索兜底 |
| 检索查不到隐藏目录下的文件 | ripgrep 默认跳过隐藏目录——**检索必须给显式路径** |
| 命令返回错误文本／无响应 | 不等于未执行：先回读页面状态，不盲目重跑（重复提交会弄掉会话） |
| 收尾校验报「开工体检留痕缺失」 | 本轮开工没跑体检，或跑了但没带 `--record-trace`——**现在补**：`init-workspace.py --check --record-trace --project "<项目根>"`，跑一次留痕即生成（§4.1 第 0 步） |
| 单次 eval 写入字段过多 → 执行超时 | 超时不代表未执行，先回读；拆成 5–8 字段一批 |
| 页面看着有值，保存后却空了 | 读的是 `input.value` 而非框架状态；改读 fiber／scope，或改用真实键入重写 |
| 「保存」类按钮点完没报错，服务端数据却没变 | `browser_click_element` 走 JS 回退时不触发框架处理器（返回值反而写 `Clicked via encodedId JS fallback`，看着像成功）——**保存／暂存／提交一律用 `find_and_act --by text --value "<按钮文本>" --action click --exact` 真实点击**；判据只能是「另开标签重载后回读」（§3.4） |
| 自定义下拉点不开、选项一闪就没 | 选项在 body 级 portal 动态渲染，跨调用即关闭——**不模拟鼠标**，走组件实例数据入口 |
| 按文本匹配元素全部落空 | `innerText` 对未进入视口的元素返回空串，改用 `textContent` |
| 动态增删字段填错区块 | 序号定位随重渲染失效——按行 label 定位 |
| 命令不报错、却落到别的页面（含使用者的页面） | 会话组未建立／会话游标被切走（使用者同时用浏览器）时，**页面级命令不报错，直接落到浏览器当前激活标签**。**动作前一律先跑一次只读 eval 读 `location.href` 核对落点**；被切走就先 `browser_tab_switch` 拉回 |
| 含中文的脚本经命令行传参变乱码 | 脚本走 **base64**（`browser_eval_content_js --script <base64> --base64`）；其余命令用 `subprocess.run([...])` 以**列表传参** |
| 升级后浏览器「不见了」、命令一律 60 秒超时 | 见 §3.0.4（升级流程杀浏览器且修复必败、`install` 检测假阳性）；处置＝请使用者用 §3.0.1 启动器重开，不要反复重试命令 |
| 命令恒报「浏览器未启动，尝试启动」、目标页被洗回首页、标签 ID 反复递增 | 浏览器其实**没在运行**，是 CLI 每条命令临时拉起一个、命令结束即回收。**先只读查进程表**（§1.0 ② 段）确认；不在就停下来请使用者双击启动器，**不自行拉起**（§3.0／§3.0.3） |
| 启动器双击后报乱码／假命令（如 `'紙--...' 不是内部或外部命令`） | `.bat` 里写进了非 ASCII（多半是中文注释／UTF-8 存盘）——`cmd.exe` 按 ANSI(GBK) 解析，注释行被撕成碎片当命令执行。**改写为纯 ASCII**，或直接用 `init-workspace.py --make-launcher` 重新生成（§3.0.1） |
| 刷新／求值报 `a dialog is currently open` | 页面开了 `beforeunload` 之类的对话框，**其后命令全部失效**——先 `browser_dialog --action accept`（或 `dismiss`）处理，再重跑原命令。**该对话框不反映脏态**（未保存时弹、已保存后同样弹），别拿它推断「存上没有」——保存后回读一律**另开标签重载**（§3.4） |
| 上传控件无法赋值 | 见 §3.4「上传处置」——试探不通过就请使用者手动上传，不擅自换通道顶替 |

### 6.3 包内机械校验（每轮跑运行态档；改封装体跑封装体档）

```bash
python scripts/verify-package.py                     # 【每轮档·默认】运行态第十段 ＋ 模板自证
python scripts/verify-package.py --ledger <台账.md>  # 同上；指定台账路径（不指定则自动探测）
python scripts/verify-package.py --package           # 【封装体档】九段全跑；改过封装体才需要
python scripts/verify-package.py --apply <投递台账.xlsx> # 第十段里对账轮锚（可省，自动就近找）
```

**两档怎么选**（2026-09-23 补）：九段里有 7 段查的是「封装体自身是否自洽」——索引↔磁盘一致、交叉引用不悬空、包内无个人数据、无本机绝对路径、编码与表格健康——这些是**维护者项**，对「这次填报对不对」零贡献；每轮都跑，等于把收尾成本压在维护面上。所以：**每轮收尾只跑默认档**（运行态第十段 ＋ 模板自证），**改过封装体（手册／配方／脚本／模板）时才加 `--package`** 跑全量。两档的退出码语义相同：**exit 1 即结构异常，先修再收工**。

前九段（**包是否自洽**）：①手册一级章节（§一–§六）齐全 ②frontmatter 完整且**版本号只在 frontmatter 一处** ③§4.7 配方索引 ↔ `recipes/` 目录**双向**一致，**手册正文里出现的每一处 `recipes/<名>` 指针都要落在磁盘上**（判据表里的指针也算——索引表是双向查的，正文指针曾是盲区），且配方**内部**以表格点名的随包脚本真的在、占位符声明与脚本实际一致 ④各配方 frontmatter 存在且 `name` 与目录名一致 ⑤启动器模板护栏在位（缺扩展加载参数＝必然 60 秒超时，直接拦；只看**有效指令行**，注释里的同名串不算；护栏判**控制流可达**——须有指向 `:NOCONFIG` 的条件跳转指令行 ＋ 标签行 ＋ 标签段内 `exit /b 1` 三者齐备，只剩字样不算；本项两轮栅栏加固见变更档案） ⑥交叉引用不悬空（手册正文**与配方内**的 § 引用；§X.Y 的节与节内条目号都要真实存在——配方用带点号指手册的节、用单数字指自己的节。**外加包根文档**：`README.md` 这类根级 `.md` 里指向手册的 § 引用，同样必须落在手册真实的节与条目上——原先只扫手册与配方，根级上手文档的引用会随手册改节号**静默失效**，两侧校验器都不报；`templates/` 不在此列，模板里的 § 号指生成到使用者的项目级要求文件，拿本手册的标题去比对必是假阳。2026-09-22 补） ⑦个人数据与单环境口径扫描（手机号／身份证／邮箱模式 ＋ 部署者自建词表 ＋ **双端术语**：假设存在第二处部署环境的措辞——本包面向单环境接收方，源环境的措辞随同步流回即 FAIL，2026-09-22 补） ⑧本机／环境专属绝对路径 ⑨配方通道状态声明齐备 ＋ 编码健康（无 U+FFFD）＋ Markdown 表格列数一致（单元格里的裸 `|` 必须写成 `\|`，否则渲染时整行错列）＋ **分隔行格数必须等于表头格数**（不符时多数渲染器把整块当普通段落，压根不成表）＋ **游离表格行**（以竖线开头、整块却无表头分隔行的连续块——渲染器当普通段落、表格静默断裂；判据只在块首报一次，且要求块内每行竖线数 ≥2、块内确无分隔行，防正文偶发竖线误报；2026-09-23 补）。

**第九段的末尾另挂「初始化能力自证」（2026-09-20 补）**：§1.5 的初始化脚本与模板本身也要被查，否则"新环境能不能建起来"这件事无人验——只验证封装体自洽、而"照 §1.5 建的新环境是绿的"没人验，正是 §5.4 记过的同一类盲区。两项：**（a）初始化脚本在位**（`scripts/init-workspace.py` 存在）**且只读封装体**（其中不得出现把生成物写回 `ROOT`／封装体自身的语句）；**（b）模板占位符闭合**——`templates/CODEBUDDY.md` 里出现的每个 `__XXX__` 都要在脚本的替换表里出现，反之脚本替换的键都要在模板里真的存在。两边对不上，要么残留占位符产出坏文件、要么替换静默失效，而两种都不会有人发现。**（c）模板机制数值 ↔ 校验判据一致**（2026-09-22 补）：机制数值（待验池上限、三档域阈值、待验表列定义）集中在 `verify-package.py` 的 `MECH` 一处，`templates/CODEBUDDY.md` 与附录 A／B 的两个模板必须与它一致，不符即 FAIL——原先两处各写一份（§5.1 模板写「上限 20」、第十段却判「>30」），分叉时两侧都不报、全绿。**（d）模板内脚本引用可解析**（2026-09-22 补）：`templates/CODEBUDDY.md` 里所有 `scripts/<文件名>` 形式的引用，必须在封装体 `scripts/` 目录下真实存在，否则 FAIL——模板会生成进新环境、成为项目级要求文件，它引用的脚本不存在时，使用者拿到的就是**指向空气的收尾校验指令**。实测缺口：模板原先两处要求跑环境侧 `verify-ledger.ps1`，而该文件包内不提供、初始化不生成、体检也不查（此缺口在源环境未暴露，只因源环境自己就有那支脚本）。

**第十段（运行态，可选，只读）：闭环是否还在正常工作。** 台账按配置项 6 由使用者自建、**不随包**，所以前三段查不到它——第十段补上：轮次基准可解析且轮号连续、待验表**表头↔分隔行格数一致**（不符即整块不成表）与**七列**（条目｜域｜登记轮｜关联次数｜固化｜待验原因｜触发条件）不变量、**域**（先校形状、再校是否落在 流程域／框架域／系统域 三档内）与**固化**（未固化／已达门槛（待并入 X）／已并入（X）；「已并入」却仍在表内＝该清出未清出）合法性、关联次数 ≤ 空转、到期判定（**按域阈值分档**：流程域 4／框架域 6／系统域 10；固化＝已达门槛者豁免；**第二支「反复不复现」型**＝关联 ≥2 且 空转 ≥ 域阈值 且 未固化；**两支都改退出码**——「有没有到期行未处置」是机械判据，留着即闭环停摆；而**「降级休眠还是清出（已入册）」**取决于该条是否已写进正文、属语义判断，故脚本同时对每条到期行解析其「拟入层级」路径、在指向的文件里检索该条摘要的关键词并给**对账提示**：全部命中即提示「疑似已收录，核实后记『清出（已入册）』」，部分命中提示人工核对，全不命中才提示「未收录、按到期降级入休眠区」）、池量（上限 **20**；超出即 WARN）、**投递记录对账**（序号连续性＝锚点是否断裂）＋**未复核轮自动判定**（「不计入范围」只认两种写法——`无` 或 `序号 a-b`；一轮未登记的初态下该行留说明性占位不报错，免得新建环境照 §5.1 模板建完就红）；台账内**任意表格**的列数与分隔行格数也在本段一并查；**条目登记簿 ↔ 事件流一致性**（台账旁找到 `EXPERIENCE-CHANGELOG.md` 时启用：①未登记 ID＝事件流有、登记簿无；②悬空 ID＝状态「在册」却没有任何事件行；③登记簿「事件数」必须等于事件流＋留档区已折叠行的实际行数——折叠只移不删、计数守恒；④按登记簿状态、**「类」列（只计「类＝条目」的在册 ID，机制类部件 ID 排除）**与「抖动命中」节声明的窗口日期**复算漂移条数与固化率**，与档案两节所填数字比对，对不上即 FAIL——这几项是机械判据，**改退出码**，与「到期对账提示」的只提示不同）；**留档区三项**（2026-09-22 补：① 待验表 ↔ 休眠／已删除区**互斥**——同一条不得既挂待验又在归档区；② 留档区各表的行格数须与本表表头一致，否则整行错位；③ **休眠区唤醒检查**——休眠条目的「域」括注命中本轮身份即提示按「唤醒在到期之后」唤醒回表并关联 +1，标「反复不复现」者除外、不唤醒）；自动就近找投递记录时**排除备份件与副本**（名含 `bak`／`backup`／`副本`／`copy`／`存档`），正本名 `投递台账.xlsx` 优先——否则会拿旧副本对账，把新锚点的序号误判为不存在。台账路径取自 `--ledger` 参数 ＞ 环境变量 `VERIFY_LEDGER` ＞ 常见位置自动探测；找不到就跳过，**不算失败**。

```bash
python scripts/verify-package.py --ledger "/path/to/EXPERIENCE-LEDGER.md"
```

**黑名单不随包**：脚本内不写任何个人词（写进去就违反了 §二"不携带个人数据"）。部署者要扫自己的专属词，自建 `scripts/verify-deny.txt`（一行一词，**不随包**）或用 `--deny`。

**为什么有这段**：其余各段靠人照做，唯独"索引与磁盘是否一致、引用是否悬空、包内是否混进个人数据、轮的锚点有没有断"这类事，人眼每轮都会漏；它就是封装体的"exit 1 不许收工"。**扫描范围（非交付物不扫）**：全包扫描只扫**交付物**——`__pycache__`（解释器字节码缓存）与 `.git`／`.svn`／`.hg`（版本控制元数据）一律排除。把仓库元数据当交付物扫只会产出成片误报：2026-09-23 实测，包目录建仓后第⑧段报 `.git/config` 疑似邮箱、第⑨段报 `.git/objects/*` 编码无法解析，封装体档从 FAIL 0 变 FAIL 50。**注意随之而来的边界**：排除后，只存在于 git 历史、已从工作区删掉的内容**扫不到**——发布前如需清历史，另行用 git 侧的检查。

**没有第十段，这套闭环只能宣称"人可照做"；有了它，才能宣称"也可被机器查证"。**


## 附录

> 本附录只放**要复制出去的模板块**（经验台账／变更档案），由 `scripts/init-workspace.py` 按模板块标题就地抽取（§1.5）；**投递台账是 xlsx**，由脚本内建生成，附录 C 只给列结构——它们**不算正文要求**，改动不走 §5.4 的要求层权限，但仍须跑 §6.3 校验。**别在这里改结构**：待验表列定义、三档域阈值与池量上限都受 §6.3 第⑨段(c) 与校验器的机制数值表约束。

### 附录 A、经验台账模板

**台账模板**（新环境可直接复制建文件）：

```markdown
# 经验台账（规则有效性判定依据）
> 作用：记录各条经验在实际任务中的表现，作为"保留 / 修订 / 删除"的判定依据；规则正文只写要求，证据写在这里。
> 格式：三节——「待验」7 列（条目 | 域 | 登记轮 | 关联次数 | 固化 | 待验原因 | 触发条件）、「本轮复核结论」4 列并按轮分段（轮次与日期写在分段标题上）、「抖动标记」为散文式说明。
> 触发：每次任务收尾，或单次会话出现 ≥3 次「踩坑→修正」回路时追加。

> **档位**：轻量（默认）。把本行改成「完整」并按完整档补齐各节即升档；校验器按本行决定查什么（§5.1 档位段）。
> **轻量档强制项**：投递台账、轮次基准、每轮复核结论、事件流（含落点登记簿对账）。
> **完整档专用（轻量档可整段跳过）**：下列「计数与口径」里的域阈值／到期判定／池量上限／休眠区／折叠／漂移与固化率复算——轻量档不维护这些节，校验器也不判。
> **计数与口径**（本表结构**冻结**：节与列建库时定一次，之后只追加行、不改结构）：轮＝一次任务收尾的经验复核，**做了复核才**在「轮次基准」追加 `rN 日期 名称`，同一会话只计一轮；台账有行而本轮未做复核的＝未复核轮，自动判定、不推进任何计数。**域分三档并决定到期阈值**——**流程域 4**（与具体站点无关的操作或机制事实）／**框架域 6**（第三方框架层行为，换站点仍可复验）／**系统域 10**（某系统的自有组件、字段与业务口径）。空转＝当前轮 − 登记轮（现算、不逐轮维护）；关联次数＝自登记轮后该域再度出现的已复核轮数（信息性指标，不决定到期）。**到期 ＝ 空转 ≥ 域阈值 且 固化＝未固化 → 降级入变更档案「休眠区」**（退出计数，**不是删除**，正文整行搬入）；**固化豁免**——固化＝已达门槛的条目不参与到期，且不得跨轮留存（要么并入清出、要么经使用者驳回后改回未固化）。**第二支判据「反复不复现」型**——关联次数 ≥2 且 空转 ≥ 域阈值 且 未固化 → 同样降级入休眠区并标「反复不复现」，该型**不再被唤醒**（域再现也不回表）、只能被清出。**唤醒在到期之后**：同一收尾内本轮该域出现的条目即使到期也不休眠；池满归档同样**跳过本轮该域出现的行**。**休眠区不得只进不出**：① 该域再现 → 唤醒回表并关联 +1（「反复不复现」者除外）；② 确已被配方正文覆盖 → 转「已删除／清出条目」区并记事件。**到期处置优先于「判断不了不删」**（后者仅适用未到期条目）。**池满不删有效条目**：待验表 **>20 行**时把空转最大者降级入休眠区，直到不超限。**行内部分固化**：一行含多条观察时，某条命中或证伪即就地删该条、行保留，行内清空才整行清出。**本轮身份自检**：填报轮的结论节标题必须点出本轮系统名（该轮行投递渠道列的平台串至少一个出现在标题或基准名里）。**机制演进**：改判据／阈值／结构须先列依据清单并报使用者批准，依据与批准记入事件行；域阈值每满 20 轮复看；拆分出的判断点留父链（「拆分自 X」）；改数值须同步校验脚本判据。单元格禁写**竖线**。清出前先对账固化目标正文（最常见的脱节）。
> **防自身膨胀（折叠规则）**：台账只留【待验】与最近一轮结论；变更档案的「本轮指标」只留**最近 3 轮**；轮次基准**满 20 轮**时，把更早的轮次明细折叠进变更档案留档区（只读备查），基准节只留近 20 轮。三处折叠都只搬位置、不删内容。

### 收尾流程（十一步，每次任务收尾按序执行）

1. **数据回写**——新事实、新口径先写进使用者数据层（配置项 4，先列单确认），再用它填表；不许只填进某个招聘系统。
2. **逐条复核**——已有经验逐条判「命中／证伪／未触发」；被证伪的当场改或删。
3. **判门槛**——≥2 次不同任务复现，或 1 次事故背书 → 可固化；否则进待验池并附触发条件。
4. **分层落位**——跨系统通用 → 本手册；单系统手法 → `recipes/<名>/SKILL.md`；只是取值 → 数据层（§5.2 定层表）。
5. **记证据**——待验表、结论表、变更档案事件流各写一行；**事件流行第 2 格必须填该条目的条目 ID**（新条目先在变更档案「落点登记簿」登记，ID 形如 `<域>#<主题键>`、一经发出不再改）。证据不进规则正文。
6. **查 ID 变动史**——改任何条文之前，先在变更档案事件流里查该条目 ID 的历史：命中【漂移】（最近 5 个已复核轮内变动 ≥3 次）或【回摆】（删除／清出后又新增）的先按判据处置，不得把已否决的写法再改回去。
7. **抖动处置**——命中【漂移】或【回摆】→ 判定该条定义不清，只能降级或拆分（§5.3 第 4 条），不再反复改文字；**证伪单列强信号**——同一 ID 出现 ≥2 次证伪即判定义不清，按漂移同等处置，不必等到 3 次变动。
8. **同步副本**——清出前先翻固化目标正文确认已收录，避免只删台账、正文没写。
9. **轮次记账**——做了复核才追加 `rN`，并写上**投递记录里的行序号**作外部凭据；投递记录只追加、不重排、不删行、不跳号（序号一断，轮锚失效且无法修复）。
10. **处置与唤醒**——到期**先对账正文**（第十段会给提示）：已收录的记「清出（已入册）」；未收录且未固化则**降级入变更档案休眠区**（退出计数，**不删**）；池满归档同理、并跳过本轮该域出现的行。本轮出现的域若在休眠区有条目，唤醒回表并关联 +1（标「反复不复现」者除外、只能被清出）。
11. **跑校验**——`python scripts/verify-package.py`（§6.3），非零退出先修结构再收工。

### 轮次基准

- 已复核轮（**尚无**——登记格式：`rN <日期> <任务简称>（序号 X）`，**序号取投递记录（配置项 4 里的投递台账）该行的序号**，它才是轮的**外部凭据**；非填报类任务无投递行时用 `（<日期>-<简称>）`。有轮之后按此形态逐轮追加进这行括号内）
- 不计入范围：**无**（取值只有两种形态：`无`，或 `序号 a-b`（a、b 为数字）。仅交接前的历史行需声明为后者；这些行无锚点、不推进任何计数。**校验逐字解析这行**——写成别的描述性文字即算结构坏）
- **未复核轮**：投递记录里有行、本项目未做复核的轮——自动判定，不计入任何计数（机械校验第十段会把它列出来）。

## 待验（下次遇到触发条件时优先验证）
> 「拟入层级」不单列一列，写在「待验原因」格内，用〔拟入层级→<落点>〕标注；列结构须与 §6.3 第十段校验一致。
| 条目 | 域 | 登记轮 | 关联次数 | 固化 | 待验原因 | 触发条件 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 本轮复核结论
> 按轮分段：每段一个 `### <日期> <任务简称>（<rN>，序号X）` 标题，标题下接本表。
| 条目 | 表现 | 动作 | 依据 |
|---|---|---|---|
|  |  |  |  |

## 抖动标记
判据按**条目 ID** 计（ID 形如 `<域>#<主题键>`，见变更档案「落点登记簿」）：**漂移**＝同一 ID 最近 5 个已复核轮内变动 ≥3 次；**回摆**＝同一 ID 删除／清出后又新增，单次即报。命中清单与固化率由 `scripts/verify-package.py` 第十段复算（见变更档案「抖动命中／本轮指标」两节）；处置只能是降级或拆分。**分母口径（2026-09-21 用户裁定）**：登记簿设「类」列（条目／部件），**漂移与固化率都只统计「类＝条目」的在册 ID**——「类＝部件」的机制类 ID（校验器／模板／轮次基准／休眠归档／说明文档…）其变动属部件被正常使用与改进、非定义不清，只统计不触发处置、也不入分母；判据是语义（描述**部件**还是**判断点**），登记簿写死结论、不硬编码名单；旧格式无「类」列时全部按「条目」处理。**证伪单列强信号**：同一 ID 出现 ≥2 次证伪即判定义不清，按漂移同等处置（降级或拆分），不必等到 3 次变动。**改任何条文之前先查该 ID 的变动史**（收尾流程第 6 步）。
```

### 附录 B、变更档案模板

**变更档案模板**：

```markdown
# 经验变更档案（条目登记簿 ＋ 事件流）
> 作用：本档案＝**条目登记簿**（每条经验的身份与当前落点，做分母）＋**事件流**（增删改事件），用于发现同一条目反复进出（抖动），并检验「经验自检规则」本身是否有效。
> **条目 ID** 形如 `<域>#<主题键>`（域取 要求／通道／配方／数据／台账／手册／机制），**不含落点路径、一经发出不再更改**——条目换落点只改登记簿的「当前落点」；登记簿与事件流的 ID 集合双向对账（有簿无行＝悬空 ID、有行无簿＝未登记 ID，均属结构异常）。

> **档位**：与经验台账同一档位（两档都保留本节与「事件流」；「抖动命中／本轮指标／留档区」属**完整档专用**，轻量档可留空）。

## 落点登记簿（条目清单，做分母）
> **`类` 列**：`条目`＝判断点／经验（进固化率分母）；`部件`＝承载机制的工具或文件本身（校验器／模板／轮次基准／休眠归档／说明文档等），其变动多为部件被正常使用与改进、非「定义不清」，**只统计、不进固化率分母、不触发漂移处置**。判据按语义：该 ID 描述的是「判断点」还是「部件」。**漂移与固化率同用此口径**。
| 条目 ID | 类 | 当前落点 | 状态 | 首登 | 事件数 | 最近事件 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 事件流
> 格式：`日期 | 条目 ID | 条目摘要 | 动作（新增/修订/删除/降级/拆分/清出/回摆/折叠） | 依据 | 备注`

| 日期 | 条目 ID | 条目摘要 | 动作 | 依据 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 抖动命中（按 ID 机械重算，随轮更新）
> 窗口＝最近 5 个已复核轮（r<起>–r<止>，<YYYY-MM-DD>～<YYYY-MM-DD>）；此行**逐字**供校验器解析窗口，格式不得改。

| 判据 | 命中 ID | 窗口内变动 | 最近事件 | 处置 |
|---|---|---|---|---|
| 漂移 |  |  |  | 待人工降级或拆分 |
| 回摆 | （无） | — | — | — |

### 已排除（部件类，只统计不触发处置）
> 本节行**不计入**漂移行数（校验器只数本子标题之前）；列「类＝部件」的命中，留证不处置。
| 判据 | 命中 ID | 窗口内变动 | 最近事件 | 排除理由 |
|---|---|---|---|---|

## 本轮指标（机械重算，不手写）
- 固化率 ＝ 窗口内零变动的「在册」ID 数 ÷ 在册且「类＝条目」的 ID 总数（分母排除「类＝部件」的机制类 ID，与漂移同口径；事件多说明还在改，不算固化）
- 未登记 ID／悬空 ID ＝ 0／0（机械校验第十段核对）
- 证伪率 ＝ 本轮被证伪条目数 / 本轮被触发复核条目数
- 待验积压 ＝ 超过轮次阈值仍未验证的【待验】条目数（积压即强制给结论或删除）

## 留档区（自台账折叠，只读备查）

### 已折叠事件（按 ID）

> 折叠规则：同一 ID 的事件行超过 4 条时，保留最近 2 条原始行＋1 条汇总行，其余移入此处。**只移不删**，ID 与依据原样保留。

| 日期 | 条目 ID | 条目摘要 | 事件 | 依据 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### 已删除／清出条目

| 日期 | 条目 | 处置 | 依据 |
|---|---|---|---|
|  |  |  |  |

### 休眠区（池满或到期降级归档；退出计数，该域再现时唤醒回表并关联 +1）

> **出口条款（休眠区不得只进不出）**：① 该域再现 → **唤醒回表**并关联 +1；标「反复不复现」者除外——该型按第二支判据休眠、只能被清出。② 确已被配方正文覆盖 → 转「已删除／清出条目」区并记事件。

| 条目 | 域 | 登记轮 | 关联次数 | 固化 | 待验原因 | 触发条件 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |
```

### 附录 C、投递台账列结构（xlsx）

**形态：单个 `.xlsx` 文件**（默认 `info/投递台账.xlsx`），由 `scripts/init-workspace.py` **内建表头行生成**——xlsx 是二进制，无法用文本模板块承载，所以本节只给列结构（要求本体见 §2.1.5）。

| 列 | 含义 | 初态 |
|---|---|---|
| A 序号 | 行的**外部凭据**：轮的锚点，也是校验器对账的依据。**只追加**——不重排、不删行、不跳号 | 空（首次填写从 1 开始） |
| B 投递日期 | 实际投递日期 | 空 |
| C 公司 | 公司规范全称 | 空 |
| D 岗位 | 岗位名称（未定写「待使用者补录」） | 空 |
| E 投递渠道 | 系统／平台标识 | 空 |
| F 状态 | 暂存未提交／已投递／… | 空 |
| G 备注 | 录错时在此注明作废，不删行 | 空 |

**填写时点**：填写完成并回读核对通过后追加一行；正式投递后**问使用者**投了什么岗位再更新状态，**只以使用者答复为准**，不从页面推断。

**为什么是 xlsx**：① 序号列是**两侧计数与核查的公共锚点**，xlsx 的单元格类型稳定，不像 Markdown 表那样会被渲染或编辑器重排改动；② 校验器第十段的投递记录对账（序号连续性）**直读 A 列**，不依赖 Excel 或第三方库（`scripts/verify-package.py` 的 `xlsx_first_col_int`）。**列名与顺序不可改**——改了会让对账读不到序号列。
